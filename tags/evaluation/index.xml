<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>YottaDynamics Technical Notes</title><link>https://blog.yottadynamics.com/</link><description>Technical notes on AI infrastructure, Kubernetes operations, and platform engineering.</description><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 15 May 2026 03:34:58 +0000</lastBuildDate><atom:link href="https://blog.yottadynamics.com/tags/evaluation/index.xml" rel="self" type="application/rss+xml"/><item><title>Get Started Hosting Your Own LLMs Using Ollama: Your Private AI Playground</title><link>https://blog.yottadynamics.com/posts/get-started-hosting-your-own-llms-using-ollama/</link><pubDate>Tue, 28 Apr 2026 10:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/get-started-hosting-your-own-llms-using-ollama/</guid><description>Ollama is one of the fastest ways to move from cloud API dependency to local model serving on your own machine. This guide covers installation, first-run commands, model selection, Open WebUI, Modelfiles, and basic API usage.</description><category>llms</category><category>ollama</category><category>developer-tools</category><category>self-hosting</category><category>privacy</category><enclosure url="https://blog.yottadynamics.com/images/posts/get-started-hosting-your-own-llms-using-ollama.png" type="image/svg+xml"/><content:encoded><![CDATA[<p><em>A practical guide to running open models on your own machine without turning your laptop into an accidental research project.</em></p>
<hr>
<p>Running LLMs locally used to mean wrestling with CUDA versions, model conversion scripts, runtime flags, and an uncomfortable amount of trial and error. <strong>Ollama</strong> changed that. It packages model download, local inference, a clean CLI, and a simple API into one workflow that works well enough for day-one experimentation and useful enough for real local tooling.</p>
<p>If your priorities are privacy, low-latency iteration, predictable cost, or just understanding the stack you are building on top of, Ollama is one of the easiest places to start.</p>
<p>This walkthrough reflects the Ollama and Open WebUI documentation available on <strong>April 28, 2026</strong>.</p>
<hr>
<h2 id="why-ollama">Why Ollama?</h2>
<p>Ollama sits in a very useful middle ground.</p>
<p>It is simpler than assembling your own <code>llama.cpp</code> or <code>vLLM</code> workflow from scratch, but more flexible than being locked into a hosted API. For many developers, that makes it the fastest path to a private local AI environment.</p>
<p>What makes it compelling:</p>
<ul>
<li><strong>Privacy-first</strong>: your prompts, files, and outputs can stay on your machine.</li>
<li><strong>Predictable cost</strong>: after hardware and electricity, there is no per-token billing for local models.</li>
<li><strong>Low-latency iteration</strong>: you can test prompts, agents, and workflows without waiting on a remote provider.</li>
<li><strong>Simple model management</strong>: pull, run, list, remove, and customize models from one CLI.</li>
<li><strong>Built-in API</strong>: local apps can talk to Ollama over HTTP without extra glue code.</li>
<li><strong>Broad model ecosystem</strong>: the model library includes general-purpose, coding, reasoning, vision, and embedding models.</li>
</ul>
<p>If you are a developer, researcher, hobbyist, or privacy-conscious power user, this is enough to build a very capable personal AI stack.</p>
<hr>
<h2 id="step-1-install-ollama">Step 1: Install Ollama</h2>
<p>Ollama supports <strong>macOS, Windows, and Linux</strong>.</p>
<p><strong>macOS</strong></p>
<ul>
<li>Download the app from <a href="https://ollama.com/download/mac">ollama.com/download/mac</a>.</li>
<li>Install the app and let it expose the <code>ollama</code> CLI in your <code>PATH</code>.</li>
</ul>
<p><strong>Windows</strong></p>
<ul>
<li>Download the installer from <a href="https://ollama.com/download/windows">ollama.com/download/windows</a>.</li>
<li>After installation, Ollama runs in the background and the CLI is available in PowerShell, Command Prompt, or Windows Terminal.</li>
</ul>
<p><strong>Linux</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>curl -fsSL https://ollama.com/install.sh | sh
</span></span></code></pre></div><p>Verify the install:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ollama -v
</span></span></code></pre></div><p>On macOS and Windows, the desktop app typically starts the local service for you. On Linux or server-style installs, you may need to start the service explicitly:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ollama serve
</span></span></code></pre></div><p>Once running, Ollama exposes a local API on <code>http://localhost:11434</code>.</p>
<hr>
<h2 id="step-2-run-your-first-model">Step 2: Run Your First Model</h2>
<p>There are two easy ways to start:</p>
<ol>
<li>Run <code>ollama</code> to use the interactive launcher.</li>
<li>Run a specific model directly from the CLI.</li>
</ol>
<p>The most direct path looks like this:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ollama run gemma3
</span></span></code></pre></div><p>Or, if you specifically want to start with a smaller Llama-family model:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ollama run llama3.2
</span></span></code></pre></div><p>The first time you run a model, Ollama downloads it automatically and then drops you into an interactive chat.</p>
<h3 id="good-starter-models">Good starter models</h3>
<p>Your best first model depends on your hardware more than your ideology.</p>
<ul>
<li><strong><code>gemma3</code></strong>: a strong default if you want a capable general-purpose model that still runs locally.</li>
<li><strong><code>llama3.2</code></strong>: a simple starting point for smaller local deployments.</li>
<li><strong><code>qwen2.5:3b</code> or <code>qwen2.5:7b</code></strong>: strong general-purpose choices, especially if you care about coding and structured output.</li>
<li><strong><code>qwen2.5-coder</code></strong>: a solid option for code generation, refactoring, and debugging tasks.</li>
<li><strong><code>mistral</code></strong>: lightweight and fast on modest hardware.</li>
<li><strong><code>phi3</code></strong>: useful when you need something compact.</li>
</ul>
<p>List the models already on your machine:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ollama ls
</span></span></code></pre></div><p>Browse more options in the <a href="https://ollama.com/library">Ollama Library</a>.</p>
<h3 id="a-practical-note-on-quantization">A practical note on quantization</h3>
<p>If your machine is memory-constrained, smaller quantized variants are usually the right answer. A slightly smaller model that runs reliably is more useful than a larger one that swaps, stalls, or crashes.</p>
<p>As a rule, optimize for <strong>consistency first</strong>, then scale model size upward once you know your hardware can carry it.</p>
<hr>
<h2 id="step-3-learn-the-core-commands">Step 3: Learn the Core Commands</h2>
<p>These are the commands you will use constantly:</p>
<ul>
<li><code>ollama pull &lt;model&gt;</code>: download a model without starting a chat.</li>
<li><code>ollama run &lt;model&gt;</code>: run a model interactively.</li>
<li><code>ollama ls</code>: list local models.</li>
<li><code>ollama ps</code>: show models currently loaded in memory.</li>
<li><code>ollama rm &lt;model&gt;</code>: remove a local model.</li>
<li><code>ollama serve</code>: start the local Ollama server.</li>
</ul>
<p>That is enough to cover most day-to-day usage.</p>
<hr>
<h2 id="step-4-add-a-web-ui">Step 4: Add a Web UI</h2>
<p>The CLI is enough for serious work, but a browser UI is often better for experimentation, sharing, and file-based workflows.</p>
<p><strong>Open WebUI</strong> is the easiest upgrade path. It gives you a ChatGPT-style interface on top of your local Ollama instance.</p>
<p>Quick start with Docker:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>docker run -d -p 3000:8080 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --add-host<span style="color:#f92672">=</span>host.docker.internal:host-gateway <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  -v open-webui:/app/backend/data <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --name open-webui <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --restart always <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  ghcr.io/open-webui/open-webui:main
</span></span></code></pre></div><p>Then open:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-text" data-lang="text"><span style="display:flex;"><span>http://localhost:3000
</span></span></code></pre></div><p>If Open WebUI is running in Docker while Ollama runs on your host machine, point Open WebUI at:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-text" data-lang="text"><span style="display:flex;"><span>http://host.docker.internal:11434
</span></span></code></pre></div><p>Once connected, you get a much more comfortable local chat environment for comparing models, testing prompts, and working with uploaded content.</p>
<hr>
<h2 id="step-5-customize-behavior-with-a-modelfile">Step 5: Customize Behavior with a Modelfile</h2>
<p>One of Ollama&rsquo;s most useful features is the <strong>Modelfile</strong>.</p>
<p>Think of it as a reproducible recipe for a tailored assistant: base model, system behavior, runtime parameters, and example conversation structure in one file.</p>
<p>Example:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-dockerfile" data-lang="dockerfile"><span style="display:flex;"><span><span style="color:#66d9ef">FROM</span> <span style="color:#e6db74">llama3.2</span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span>SYSTEM <span style="color:#e6db74">&#34;&#34;&#34;</span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span>You are a helpful coding assistant specialized in Python and web development.<span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span>Always respond with clear, commented code examples.<span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&#34;&#34;&#34;</span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span>PARAMETER temperature 0.7<span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span>PARAMETER num_ctx <span style="color:#ae81ff">8192</span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span>MESSAGE user <span style="color:#e6db74">&#34;Explain how to build a REST API&#34;</span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span>MESSAGE assistant <span style="color:#e6db74">&#34;Sure. Here&#39;s a clean FastAPI example and why each part exists.&#34;</span><span style="color:#960050;background-color:#1e0010">
</span></span></span></code></pre></div><p>Build it:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ollama create my-coder -f Modelfile
</span></span></code></pre></div><p>Then run it:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ollama run my-coder
</span></span></code></pre></div><p>This is a very effective pattern for role-specific assistants: coding helpers, internal documentation bots, architecture reviewers, domain-specific writers, or local support tools.</p>
<hr>
<h2 id="step-6-use-ollama-from-code">Step 6: Use Ollama from Code</h2>
<p>Ollama exposes a local HTTP API, but the Python library is an easy place to start.</p>
<p>Install it:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install ollama
</span></span></code></pre></div><p>Python example:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> ollama <span style="color:#f92672">import</span> chat
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> chat(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gemma3&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Write a Python function that returns the Fibonacci sequence up to n elements.&#34;</span>,
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>print(response<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content)
</span></span></code></pre></div><p>This is enough to wire Ollama into scripts, developer tools, side projects, or local prototypes without going through a hosted API.</p>
<hr>
<h2 id="hardware-reality-check">Hardware Reality Check</h2>
<p>Local model serving is not magic. Hardware still matters.</p>
<p>Useful rough guidance:</p>
<ul>
<li><strong>8 GB to 16 GB memory</strong>: stay in the small-model range and favor compact or quantized models.</li>
<li><strong>16 GB to 32 GB memory</strong>: 7B-class models become much more comfortable.</li>
<li><strong>24 GB+ dedicated GPU VRAM or large unified memory systems</strong>: you can move into larger and more capable models with fewer compromises.</li>
</ul>
<p>Two practical rules matter more than any size chart:</p>
<ol>
<li>Smaller models that are always available beat larger models that are unstable.</li>
<li>Context length and quantization settings can matter just as much as parameter count.</li>
</ol>
<p>If performance feels wrong, check <code>ollama ps</code> first. It is the fastest way to confirm whether a model is fitting in GPU memory, CPU memory, or both.</p>
<hr>
<h2 id="what-ollama-is-great-for">What Ollama Is Great For</h2>
<p>Once the basics are working, the common next steps are obvious:</p>
<ul>
<li>a private coding assistant</li>
<li><a href="/posts/deep-dive-retrieval-augmented-generation-rag/">local document Q&amp;A</a></li>
<li>offline experimentation on a laptop</li>
<li>prompt testing without API cost</li>
<li>lightweight internal tools for developers or analysts</li>
<li>evaluating open models before <a href="/posts/hosting-local-llms-on-kubernetes/">building a larger self-hosted platform</a></li>
</ul>
<p>For many people, Ollama is not the final production serving layer. It is the fastest way to learn what your workloads actually need before you commit to a heavier stack.</p>
<hr>
<h2 id="final-take">Final Take</h2>
<p>If you want to explore local AI without spending a week assembling plumbing, Ollama is one of the best on-ramps available right now.</p>
<p>Install it, pull a model, and start with something small and reliable. Add Open WebUI when you want a better interface. Reach for Modelfiles when you want repeatable custom behavior. Move to heavier infrastructure only after you understand your real workload.</p>
<p>That sequence will teach you more than reading another dozen model benchmark threads.</p>
<h3 id="useful-links">Useful links</h3>
<ul>
<li><a href="https://docs.ollama.com/">Ollama Docs</a></li>
<li><a href="https://ollama.com/library">Ollama Library</a></li>
<li><a href="https://docs.ollama.com/modelfile">Modelfile Reference</a></li>
<li><a href="https://docs.openwebui.com/">Open WebUI Docs</a></li>
</ul>
]]></content:encoded></item><item><title>Observability for AI Agents: Logs, Traces, and Metrics That Actually Tell You Something</title><link>https://blog.yottadynamics.com/posts/observability-for-ai-agents/</link><pubDate>Fri, 17 Apr 2026 11:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/observability-for-ai-agents/</guid><description>Monitoring an agent is not the same as monitoring a service. The question shifts from whether it is running to whether it is reasoning correctly — and that requires a different observability stack built around structured traces, quality metrics, and cost attribution.</description><category>ai-agents</category><category>operations</category><category>observability</category><category>ai-infrastructure</category><enclosure url="https://blog.yottadynamics.com/images/posts/observability-for-ai-agents.png" type="image/svg+xml"/><content:encoded><![CDATA[<p>An agent can pass every health check, return HTTP 200 on every request, and stay within p95 latency targets — while quietly producing worse and worse output for weeks. By the time the degradation shows up in aggregate business metrics, users have already formed an opinion.</p>
<p>Traditional monitoring answers the wrong question. &ldquo;Is it running?&rdquo; is not the question that matters for agents. The question is &ldquo;is it reasoning correctly?&rdquo; — and that requires a fundamentally different observability stack.</p>
<p>This post covers what that stack looks like: the three pillars (logs, traces, metrics), what to capture at each layer, the specific failure modes each one surfaces, and the order in which to build it.</p>
<hr>
<h2 id="why-infrastructure-monitoring-misses-agent-failures">Why Infrastructure Monitoring Misses Agent Failures</h2>
<p>A standard APM setup — uptime checks, error rates, latency histograms, resource utilization — is built for a model where correct behavior means reaching a defined endpoint without throwing an exception. For deterministic services, that&rsquo;s sufficient. A function that returns the right output without errors is a function that&rsquo;s working.</p>
<p>Agents break this model in three specific ways:</p>
<p><strong>Silent quality degradation.</strong> The agent completes every task without errors. Latency is fine. Token counts are normal. But the output quality has been drifting downward for two weeks because a prompt change shifted how the model interprets ambiguous inputs. No infrastructure metric captures this.</p>
<p><strong>Correct output via wrong path.</strong> The agent answers the question correctly but made seven tool calls when three were appropriate, retrieved three irrelevant documents before finding the right one, and took 4.2 seconds when 0.8 was achievable. The final answer looks fine; the execution was expensive and fragile. Infrastructure monitoring shows nothing anomalous.</p>
<p><strong>Reasoning loops and dead ends.</strong> The agent gets stuck in a loop — checking the same tool repeatedly, generating intermediate reasoning that contradicts earlier steps, or failing to recognize that it has enough information to answer. Eventually it times out or produces a degraded response. The timeout shows up as latency; everything upstream is invisible.</p>
<p>The common thread: <a href="/posts/why-agents-fail-in-production/">agent failure modes</a> are failures of <em>reasoning quality</em>, not execution health. You need instrumentation that captures the decision-making process, not just its infrastructure envelope.</p>
<hr>
<h2 id="pillar-1-structured-logs">Pillar 1: Structured Logs</h2>
<p>Logs are the atomic unit of agent observability. For traditional services, a request log captures the input, output, status code, and duration — enough to reconstruct what happened. For agents, you need the full decision record at every step.</p>
<h3 id="what-to-log">What to log</h3>
<p>Every agent execution generates a sequence of events. Each should be a structured log entry with a consistent schema:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;trace_id&#34;</span>: <span style="color:#e6db74">&#34;agt-7f3a9b2c&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;span_id&#34;</span>: <span style="color:#e6db74">&#34;span-0041&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;timestamp&#34;</span>: <span style="color:#e6db74">&#34;2026-04-17T14:32:01.847Z&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;event_type&#34;</span>: <span style="color:#e6db74">&#34;tool_call&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;agent_id&#34;</span>: <span style="color:#e6db74">&#34;research-agent-v2&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;session_id&#34;</span>: <span style="color:#e6db74">&#34;sess-8821&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;tool_name&#34;</span>: <span style="color:#e6db74">&#34;web_search&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;tool_input&#34;</span>: { <span style="color:#f92672">&#34;query&#34;</span>: <span style="color:#e6db74">&#34;Q3 earnings Apple 2025&#34;</span> },
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;tool_output_summary&#34;</span>: <span style="color:#e6db74">&#34;Retrieved 5 results, top result: ...&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;latency_ms&#34;</span>: <span style="color:#ae81ff">312</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;token_cost&#34;</span>: <span style="color:#66d9ef">null</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;status&#34;</span>: <span style="color:#e6db74">&#34;success&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;reasoning_snapshot&#34;</span>: <span style="color:#e6db74">&#34;Need recent earnings data to answer the user&#39;s comparison question.&#34;</span>
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>The <code>reasoning_snapshot</code> field is the one most teams omit. It captures <em>why</em> the agent made this call at this moment — the intermediate reasoning that led to the decision. Without it, you have a log of what the agent did but no record of why. Debugging becomes guesswork.</p>
<p>Key event types to capture:</p>
<ul>
<li><strong><code>task_start</code></strong> — initial goal, session context, model version, tool set</li>
<li><strong><code>reasoning_step</code></strong> — intermediate chain-of-thought, current plan state</li>
<li><strong><code>tool_call</code></strong> — tool name, full input, output summary, latency, status</li>
<li><strong><code>tool_error</code></strong> — failure type, error message, retry attempt number</li>
<li><strong><code>context_update</code></strong> — what was added to or trimmed from the context window</li>
<li><strong><code>task_complete</code></strong> — final output, total latency, total tool calls, total token cost, completion status</li>
<li><strong><code>task_abort</code></strong> — reason, last successful step, partial output if any</li>
</ul>
<h3 id="log-levels-and-sampling">Log levels and sampling</h3>
<p>In early production, default to logging everything. Storage is cheap; debugging with insufficient logs is expensive.</p>
<p>At scale, log sampling becomes necessary. A reasonable strategy:</p>
<ul>
<li><strong>Always log</strong>: <code>task_start</code>, <code>task_complete</code>, <code>task_abort</code>, <code>tool_error</code></li>
<li><strong>Sample at 10–20%</strong>: <code>reasoning_step</code>, <code>tool_call</code> for successful high-volume tasks</li>
<li><strong>Always log on quality alert</strong>: If a quality monitoring signal fires on a session, retroactively promote the full trace to permanent storage</li>
</ul>
<p>Structure your logs so this promotion is possible — store the full execution trace in a short-lived buffer (Redis with 24-hour TTL works well) and only persist to long-term storage for flagged sessions or sampling hits.</p>
<hr>
<h2 id="pillar-2-distributed-traces">Pillar 2: Distributed Traces</h2>
<p>Logs give you the events. Traces give you the story — a causally connected sequence of events that follows a single task from the initial query to the final response, across every service boundary it crosses.</p>
<p>For agents, traces are what let you answer: &ldquo;Exactly what happened during this specific run, in what order, and what caused the final output?&rdquo;</p>
<h3 id="trace-structure">Trace structure</h3>
<p>A trace is a tree of spans. Each span represents a named operation with a start time, duration, status, and attributes. Spans nest: the root span is the full task; child spans are individual steps; tool calls that reach external services are leaf spans.</p>
<pre tabindex="0"><code>[root] task: &#34;summarize Q3 earnings for AAPL and MSFT&#34;   4.2s
  ├── [span] reasoning: plan decomposition                 0.1s
  ├── [span] tool_call: web_search(&#34;AAPL Q3 2025&#34;)        0.3s
  │     └── [span] http: api.search.example.com            0.28s
  ├── [span] tool_call: web_search(&#34;MSFT Q3 2025&#34;)        0.4s
  │     └── [span] http: api.search.example.com            0.38s
  ├── [span] tool_call: retrieve_doc(&#34;AAPL-10Q-Q3-2025&#34;)  1.1s
  │     └── [span] vector_search: doc-store                0.09s
  ├── [span] reasoning: synthesis                          0.2s
  └── [span] llm_call: generate final response             2.1s
</code></pre><p>This trace immediately reveals that the LLM generation step is consuming half the total latency. It also shows whether tool calls are running sequentially when they could run in parallel — a common optimization opportunity invisible without tracing.</p>
<h3 id="context-propagation">Context propagation</h3>
<p>Context propagation is what makes distributed tracing work. A unique <code>trace_id</code> must flow through every component the task touches: the agent runtime, every tool call, every external API, every sub-agent in a multi-agent workflow.</p>
<p>OpenTelemetry is the standard here. Its W3C <code>traceparent</code> header propagates trace context across HTTP boundaries automatically when your tools and services are instrumented. For internal function calls and LLM API calls, propagate the trace context manually through your SDK calls.</p>
<p>The consequence of not doing this: you can trace within the agent but not across tool boundaries, which is exactly where the interesting failures happen.</p>
<h3 id="multi-agent-trace-propagation">Multi-agent trace propagation</h3>
<p>In <a href="/posts/ai-agent-architecture-memory-tools-orchestration/">multi-agent systems</a>, each agent spawns its own trace — but those traces need to be linked. When an orchestrator delegates a subtask to a worker agent, pass the parent trace ID and span ID as task metadata. The worker agent creates a new root span with <code>parent_span_id</code> set to the orchestrator&rsquo;s span. This links the full execution graph without collapsing it into one monolithic trace.</p>
<p>The result: you can view the orchestrator&rsquo;s trace and drill into any delegated subtask, or query by trace ID to see the full multi-agent execution path for a given user request.</p>
<hr>
<h2 id="pillar-3-metrics">Pillar 3: Metrics</h2>
<p>Metrics aggregate individual events into signals you can monitor, alert on, and trend over time. For agents, the metrics that matter form a four-level hierarchy.</p>
<h3 id="level-1-business-metrics">Level 1: Business metrics</h3>
<p>These answer &ldquo;is the agent accomplishing what it&rsquo;s supposed to accomplish?&rdquo; They&rsquo;re defined against your specific use case, not derived from infrastructure.</p>
<table>
  <thead>
      <tr>
          <th>Metric</th>
          <th>Definition</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Goal completion rate</strong></td>
          <td>% of tasks where the agent fully completed the stated objective</td>
      </tr>
      <tr>
          <td><strong>User acceptance rate</strong></td>
          <td>% of outputs accepted without correction or retry</td>
      </tr>
      <tr>
          <td><strong>Task completion time</strong></td>
          <td>Wall-clock time from task start to accepted completion</td>
      </tr>
      <tr>
          <td><strong>Cost per completed task</strong></td>
          <td>Total token spend + external API cost per successful task</td>
      </tr>
  </tbody>
</table>
<p>Goal completion rate is the single most important metric and the hardest to instrument. It requires a definition of &ldquo;complete&rdquo; that your system can evaluate automatically — either a structured output schema the agent fills, an LLM-as-judge scorer, or an explicit user acceptance signal (thumbs up, edit rate, retry rate).</p>
<h3 id="level-2-quality-metrics">Level 2: Quality metrics</h3>
<p>These answer &ldquo;how well is the agent reasoning?&rdquo;</p>
<table>
  <thead>
      <tr>
          <th>Metric</th>
          <th>Definition</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Tool precision</strong></td>
          <td>% of tool calls that were necessary for task completion</td>
      </tr>
      <tr>
          <td><strong>Tool recall</strong></td>
          <td>% of tasks where the agent called all necessary tools</td>
      </tr>
      <tr>
          <td><strong>First-attempt success rate</strong></td>
          <td>% of tasks completed without retry or fallback</td>
      </tr>
      <tr>
          <td><strong>Reasoning coherence score</strong></td>
          <td>LLM-judge score on intermediate reasoning quality (sampled)</td>
      </tr>
      <tr>
          <td><strong>Hallucination rate</strong></td>
          <td>% of outputs containing factually incorrect claims (sampled, human or judge)</td>
      </tr>
  </tbody>
</table>
<p>Tool precision and recall are the leading indicators for trajectory failure — they move before final output quality degrades, giving you an early warning signal.</p>
<h3 id="level-3-operational-metrics">Level 3: Operational metrics</h3>
<p>These answer &ldquo;is the agent behaving within expected operational parameters?&rdquo;</p>
<table>
  <thead>
      <tr>
          <th>Metric</th>
          <th>Definition</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Average tool calls per task</strong></td>
          <td>Total tool calls / completed tasks. Baseline this at launch; deviations signal trajectory drift.</td>
      </tr>
      <tr>
          <td><strong>Context window utilization</strong></td>
          <td>Average % of context window used at task completion. High utilization predicts truncation failures.</td>
      </tr>
      <tr>
          <td><strong>Loop iteration count</strong></td>
          <td>Average ReAct iterations per task. Spikes indicate reasoning loops or stuck states.</td>
      </tr>
      <tr>
          <td><strong>Tool error rate by type</strong></td>
          <td>Errors broken down by tool and error class. Identifies specific tool reliability issues.</td>
      </tr>
      <tr>
          <td><strong>Retry rate</strong></td>
          <td>% of tool calls that required retry. Elevated rates indicate external API instability.</td>
      </tr>
  </tbody>
</table>
<h3 id="level-4-infrastructure-metrics">Level 4: Infrastructure metrics</h3>
<p>Standard service metrics — latency p50/p95/p99, token throughput, model API error rate, queue depth. These are necessary but not sufficient on their own.</p>
<p>One important addition specific to LLMs: <strong>decomposed latency</strong>. Total response time breaks into:</p>
<ul>
<li><strong>Time to first token (TTFT)</strong>: Model inference start latency. Dominated by prompt processing time.</li>
<li><strong>Inter-token latency (ITL)</strong>: Throughput during generation. Affected by model size and serving infrastructure.</li>
<li><strong>Tool execution time</strong>: Latency attributable to external calls. Separate this from model latency or you can&rsquo;t diagnose the actual bottleneck.</li>
</ul>
<p>Most production agents have a counterintuitive latency profile: the model itself is fast; the bottleneck is tool calls. Without decomposed latency, you&rsquo;d never know where to optimize.</p>
<hr>
<h2 id="alerting-what-to-watch-and-when">Alerting: What to Watch and When</h2>
<p>Metrics without alerts are a dashboard you check after something breaks. The alerts that matter:</p>
<p><strong>Goal completion rate drop &gt; 5% vs. 7-day rolling average</strong> — Immediate investigation. This is the primary quality signal.</p>
<p><strong>Tool call efficiency ratio &gt; 1.5x baseline</strong> — The agent is making more calls than usual to complete the same tasks. Leading indicator for trajectory drift or context window problems.</p>
<p><strong>Context window utilization &gt; 80%</strong> — Truncation is imminent. Review the tasks hitting this threshold; they&rsquo;re the ones most likely to fail unpredictably.</p>
<p><strong>Loop iteration p95 &gt; 2x baseline</strong> — Reasoning loops are forming. Something in the execution path is causing the agent to re-examine decisions it already made.</p>
<p><strong>LLM-judge quality score 7-day trend &lt; -0.1</strong> — Gradual quality drift. Not a spike, but a persistent downward movement that will compound if not addressed.</p>
<p><strong>Cost per task &gt; 150% of budget</strong> — Either a task complexity spike or a trajectory failure causing excessive tool calls.</p>
<p>Set these as relative thresholds against rolling baselines, not absolute values. Absolute thresholds go stale as task mix evolves; relative thresholds adapt automatically.</p>
<hr>
<h2 id="the-reasoning-visibility-problem">The Reasoning Visibility Problem</h2>
<p>The hardest part of agent observability is capturing why the agent made a decision, not just what it decided.</p>
<p>LLMs don&rsquo;t expose their internal reasoning as structured data — they generate natural language. Capturing useful reasoning visibility requires deliberate prompt design: instruct the model to externalize its reasoning at key decision points in a structured format before taking action.</p>
<p>A ReAct-style prompt already does this partially — each thought step is a reasoning trace. Make it more useful by asking for explicit structure:</p>
<pre tabindex="0"><code>Before calling any tool, output:
REASONING: [why this tool, why now, what you expect to learn]
TOOL: [tool name]
INPUT: [parameters]
</code></pre><p>This gives you log-parseable reasoning snapshots at every tool call — not a perfect window into model internals, but enough to answer &ldquo;why did it do that?&rdquo; for the majority of cases.</p>
<p>For tasks where reasoning quality is particularly important (high-stakes decisions, multi-step workflows), run a post-execution reflection prompt: ask the model to review its own trace and identify any steps where a different decision would have been better. Store the reflection as a <a href="/posts/how-memory-works-in-ai-agents/">semantic memory</a> entry and log it against the trace ID.</p>
<hr>
<h2 id="the-tooling-landscape">The Tooling Landscape</h2>
<p>You don&rsquo;t need to build this from scratch. The observability tooling for LLM applications has matured significantly:</p>
<p><strong>Tracing and logging</strong>: OpenTelemetry is the foundation — instrument your agent with the OTel SDK, export to your existing backend (Jaeger, Tempo, Datadog, Honeycomb). LangSmith and Langfuse provide purpose-built tracing UIs with LLM-specific span attributes, token cost tracking, and built-in LLM-as-judge evaluation. Helicone operates as a proxy layer with minimal integration cost.</p>
<p><strong>Metrics and dashboards</strong>: Standard Prometheus + Grafana works if you define agent-specific metrics properly. Both LangSmith and Langfuse expose metric APIs you can feed into existing dashboards.</p>
<p><strong>LLM-as-judge pipelines</strong>: RAGAS provides evaluation metrics for retrieval quality. For general quality scoring, a judge prompt running against sampled outputs via the Anthropic or OpenAI API with structured output schemas integrates cleanly into any pipeline.</p>
<p>The choice between purpose-built LLM observability tools and extending your existing stack depends on your team&rsquo;s infrastructure preference. Purpose-built tools are faster to get value from; extending your existing stack keeps your observability unified. For most teams starting out, purpose-built wins on time-to-insight.</p>
<hr>
<h2 id="what-to-build-first">What to Build First</h2>
<p>The order matters. This is the sequence that gets you to production-ready observability fastest:</p>
<p><strong>Day 1: Structured logging on every tool call.</strong> Log the full request-response cycle with trace ID, tool name, input, output summary, latency, and status. This is the minimum needed to debug anything.</p>
<p><strong>Day 2–3: End-to-end tracing.</strong> Wire up OpenTelemetry or a purpose-built tool. Every task gets a trace ID that propagates through the full execution path. You can now reconstruct any execution after the fact.</p>
<p><strong>Week 1: Business metrics defined and instrumented.</strong> What does task completion look like for your specific use case? Define it, build the instrumentation, baseline it before launch. You need a starting line.</p>
<p><strong>Week 2: Quality metrics and alerting.</strong> Tool precision/recall, loop iteration count, context window utilization. Alerts on the thresholds above. This is what catches silent degradation before it compounds.</p>
<p><strong>Ongoing: Reasoning snapshots.</strong> Prompt the model to externalize its reasoning at decision points. These make debugging dramatically faster and are cheap to add once the logging infrastructure is in place.</p>
<p>The feedback loop this enables: production monitoring surfaces an anomaly → trace pinpoints the execution step → log captures the reasoning at that step → you understand exactly what went wrong → you fix it and add a regression test. Without all three layers, any step in that chain becomes guesswork.</p>
<hr>
<h2 id="the-observability-gap-is-an-operational-risk">The Observability Gap Is an Operational Risk</h2>
<p>Teams that skip agent-specific observability aren&rsquo;t just flying blind on quality — they&rsquo;re accumulating operational debt that compounds with every deployment. Each change to a prompt, tool definition, or model version is a potential quality regression with no detection mechanism.</p>
<p>The investment to instrument properly is a few days of engineering work. The cost of not doing it is measured in production incidents you can&rsquo;t diagnose, quality degradation you don&rsquo;t notice until users leave, and deployments you&rsquo;re afraid to make because you don&rsquo;t know what they&rsquo;ll break.</p>
<p>Observe first. Build confidence second. Deploy without fear third.</p>
<p><em>Part of the <a href="/series/ai-agents-in-production/">AI Agents in Production</a> series.</em></p>
]]></content:encoded></item><item><title>Why Agents Fail in Production (And How to Catch It Before It Reaches Your Users)</title><link>https://blog.yottadynamics.com/posts/why-agents-fail-in-production/</link><pubDate>Fri, 17 Apr 2026 10:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/why-agents-fail-in-production/</guid><description>Non-deterministic systems require evaluation strategies that traditional QA cannot provide. Closing the gap requires a golden dataset, trajectory analysis, an LLM-as-judge pipeline, and a feedback loop that runs before every deployment.</description><category>ai-agents</category><category>operations</category><category>evaluation</category><category>ai-infrastructure</category><category>delivery</category><enclosure url="https://blog.yottadynamics.com/images/posts/why-agents-fail-in-production.png" type="image/svg+xml"/><content:encoded><![CDATA[<p>An <a href="/posts/what-is-an-ai-agent-a-practical-guide/">agent</a> that works in a demo and an agent that works reliably in production are two different systems. The gap between them is one of the most expensive lessons teams learn when they start moving agentic systems into real traffic.</p>
<p>The failure usually isn&rsquo;t dramatic. The agent doesn&rsquo;t crash. It doesn&rsquo;t throw errors. It just quietly produces worse output over time — or handles an edge case in a way no test anticipated — or makes seventeen tool calls where three would have sufficed. By the time you notice, real users have been affected, and you have no baseline to know when the degradation started.</p>
<p>This post covers what actually breaks in production agents, why traditional QA misses it, and how to build an evaluation infrastructure that catches failures before they reach users.</p>
<hr>
<h2 id="why-traditional-qa-doesnt-transfer">Why Traditional QA Doesn&rsquo;t Transfer</h2>
<p>Traditional software is deterministic. Given input A, it produces output B — every time, on every machine, in every environment. Tests are assertions: given this input, assert this output. If the assertion passes, the code works. If it fails, something is broken.</p>
<p>Agents break this model at the foundation. They&rsquo;re probabilistic by nature. The same input can produce meaningfully different outputs on different runs — not because something is wrong, but because that&rsquo;s how the underlying model works. Stochastic sampling, tool call ordering, intermediate reasoning traces — all of it introduces variance that assertion-based testing can&rsquo;t contain.</p>
<p>More importantly, even when an agent produces the &ldquo;right&rdquo; final answer, the path it took to get there may be fragile, expensive, or unreliable on a slightly different input. An agent that solves the problem via an unexpected detour might succeed 90% of the time and silently fail the other 10% — and you&rsquo;d never know from output-only evaluation.</p>
<p>This means the evaluation strategies that work for deterministic software — unit tests, integration tests, coverage thresholds — are necessary but nowhere near sufficient for agents.</p>
<hr>
<h2 id="the-production-failure-taxonomy">The Production Failure Taxonomy</h2>
<p>Before you can detect failures, you need a clear map of what failure actually looks like. Agent failures fall into six categories, and they&rsquo;re not equally visible:</p>
<h3 id="1-silent-quality-degradation">1. Silent Quality Degradation</h3>
<p>The most dangerous failure mode because there&rsquo;s no error signal. The agent completes the task, returns a result, everything looks normal — but the output quality has drifted. This happens when prompt changes, model version bumps, or tool updates subtly alter behavior in ways that aren&rsquo;t caught by output-only checks.</p>
<p>Silent degradation is why you need quality metrics running continuously, not just pass/fail gates. Without a baseline, you have no way to know when the line started moving.</p>
<h3 id="2-trajectory-failure">2. Trajectory Failure</h3>
<p>The agent reaches the correct answer via the wrong path. It uses a slower tool when a faster one was appropriate, makes five calls when two were needed, or retrieves irrelevant context that pollutes its reasoning. The final output looks fine; the execution was wasteful or unreliable.</p>
<p>Trajectory failures are invisible to output-only evaluation but show up immediately in cost and latency metrics — and they often predict future quality failures, because the detour that works today may not work tomorrow.</p>
<h3 id="3-prompt-sensitivity-failure">3. Prompt Sensitivity Failure</h3>
<p>Minor input variations produce disproportionately large output differences. An agent that handles &ldquo;cancel my subscription&rdquo; correctly but fails on &ldquo;I want to stop my subscription&rdquo; has a prompt sensitivity problem. These failures are particularly common after prompt edits, model upgrades, or tool definition changes — any modification that shifts the model&rsquo;s interpretation of similar inputs.</p>
<h3 id="4-context-window-mismanagement">4. Context Window Mismanagement</h3>
<p>Long-running tasks, multi-step workflows, or sessions with heavy tool outputs can fill the context window in ways that weren&rsquo;t anticipated. When truncation kicks in, the agent loses earlier context — sometimes including the original goal, constraints, or decisions it already made. The result is circular reasoning, repeated tool calls, or abrupt task failure mid-execution. (See <a href="/posts/how-memory-works-in-ai-agents/">how memory layers prevent this</a> for the architectural fix.)</p>
<p>This is especially common in agents that handle file-heavy workloads, long document processing, or multi-turn conversations without explicit context management.</p>
<h3 id="5-tool-misuse">5. Tool Misuse</h3>
<p>The agent calls the right tool with wrong parameters, calls the wrong tool for the job, or chains tools in an order that produces a technically valid but semantically wrong result. Tool misuse is particularly hard to catch because the tool calls themselves succeed — there&rsquo;s no exception — but the output is wrong in a way that depends on understanding the intended semantics.</p>
<p>Tool definition quality directly determines how often this happens. Ambiguous tool descriptions produce ambiguous tool use.</p>
<h3 id="6-reasoning-drift-in-multi-step-tasks">6. Reasoning Drift in Multi-Step Tasks</h3>
<p>Over long task sequences, the agent&rsquo;s reasoning can drift from the original goal. Early decisions compound into later ones; a minor misinterpretation in step 2 propagates through steps 3–8. By the end, the agent has solved a subtly different problem than the one it was given.</p>
<p>Multi-hop reasoning chains and ReAct-style loops are especially vulnerable. Each iteration introduces variance; variance compounds.</p>
<hr>
<h2 id="what-effective-evaluation-actually-covers">What Effective Evaluation Actually Covers</h2>
<p>Evaluation that catches these failure modes has three distinct layers, each addressing what the others miss.</p>
<h3 id="layer-1-capability-assessment">Layer 1: Capability Assessment</h3>
<p>Does the agent have the fundamental capabilities the task requires? Can it follow complex multi-step instructions? Reason through ambiguous inputs? Use tools correctly in isolation before they&rsquo;re chained together?</p>
<p>This layer uses structured benchmarks — both standard datasets and task-specific evaluations drawn from your actual production workload. Standard benchmarks tell you where the model lands on the capability curve; production-specific evaluations tell you whether that capability transfers to your specific use case.</p>
<p>The most common mistake here is relying only on general benchmarks and assuming they predict production performance. They don&rsquo;t. A model that excels on MMLU may still fail systematically on the specific reasoning patterns your application requires.</p>
<h3 id="layer-2-trajectory-and-tool-use-analysis">Layer 2: Trajectory and Tool Use Analysis</h3>
<p>This is the layer most teams skip entirely, and it&rsquo;s the one that catches the failure modes output-only evaluation misses.</p>
<p>Trajectory evaluation compares the actual execution path — the sequence of reasoning steps and tool calls — against an expected reference path. It measures:</p>
<ul>
<li><strong>Tool precision</strong>: Did the agent call the tools it should have called?</li>
<li><strong>Tool recall</strong>: Did it call all the tools necessary to complete the task correctly?</li>
<li><strong>Ordering</strong>: Did it use them in the right sequence?</li>
<li><strong>Parameter quality</strong>: Were the parameters meaningful and well-formed?</li>
<li><strong>Call efficiency</strong>: Did it minimize unnecessary or redundant calls?</li>
</ul>
<p>A trajectory that scores high on all five dimensions is a trajectory you can rely on. One that reaches the right answer but fails on efficiency or ordering is a fragile path that may not survive input variation.</p>
<p>Implementing trajectory evaluation means logging every tool call with its full context — what the agent was thinking when it made the call, what it received, what it decided next. <a href="/posts/observability-for-ai-agents/">Structured traces</a> (OpenTelemetry works well here) make this feasible at scale.</p>
<h3 id="layer-3-final-response-evaluation">Layer 3: Final Response Evaluation</h3>
<p>Output quality assessment — does the final response actually answer the question correctly, completely, and without hallucination?</p>
<p>Two evaluation methods, neither sufficient alone:</p>
<p><strong>LLM-as-judge</strong>: Use a capable model (typically a stronger or different model than the agent itself) to rate output quality against defined criteria: correctness, completeness, relevance, tone, and absence of hallucination. Runs at scale, no human bottleneck, catches most obvious failures.</p>
<p>The calibration problem is real: LLM judges can be fooled by confident-sounding but wrong answers, and they tend to favor verbose outputs regardless of actual quality. Mitigate this by testing your judge against a human-rated calibration set, penalizing hedged or vacuous answers explicitly in the rubric, and running adversarial examples the judge is known to struggle with.</p>
<p><strong>Human review</strong>: Accurate, catches nuanced failures, expensive to scale. Use human review as a calibration layer for the LLM judge, for high-stakes or ambiguous cases, and for systematic sampling — not as the primary evaluation channel for volume.</p>
<p>The practical approach: human review calibrates the judge; the judge runs at scale; human review audits samples of the judge&rsquo;s outputs to catch drift in judge quality. This is a loop, not a one-time setup.</p>
<hr>
<h2 id="the-golden-dataset">The Golden Dataset</h2>
<p>The golden dataset is the single most valuable evaluation asset you can build. It is a curated, representative collection of:</p>
<ul>
<li><strong>Inputs</strong>: Real or realistic task prompts drawn from your production workload distribution</li>
<li><strong>Expected outputs</strong>: Known-good reference answers with acceptable variation ranges</li>
<li><strong>Expected trajectories</strong>: Reference tool call sequences, including ordering and parameter constraints</li>
<li><strong>Metadata</strong>: Task difficulty, failure category risk, coverage tags</li>
</ul>
<p>Every CI/CD pipeline runs against the golden dataset before any change — to code, prompts, tool definitions, or model versions — reaches production. A change that degrades golden dataset performance doesn&rsquo;t ship.</p>
<p><strong>How to build it:</strong></p>
<p>Start with 50–100 examples covering your most common task types and known edge cases. Prioritize diversity over volume: a dataset of 100 well-chosen, well-annotated examples is more valuable than 1,000 examples clustered around a single pattern.</p>
<p>Annotate trajectories explicitly. For each example, define which tools should be called, in what order, with what parameter constraints. This is time-consuming. Do it anyway. Trajectory annotations are what make the dataset capable of catching the failures output annotation misses.</p>
<p>Set tiered pass/fail thresholds. Different task types have different quality tolerances. A customer support agent might require 95% output accuracy; a research assistant might tolerate more variance. Define thresholds by tier before deployment — not after the first production failure.</p>
<p><strong>How to grow it:</strong></p>
<p>Every production failure that reaches users is a test case the golden dataset should have contained. The process for adding it is: root cause the failure, create the example, annotate it, add it to the dataset, confirm the evaluation pipeline catches it, then ship the fix.</p>
<p>This transforms every production incident into a permanent regression test. Teams that run this process consistently see incident recurrence rates drop significantly within the first few months.</p>
<hr>
<h2 id="the-evaluation-pipeline-architecture">The Evaluation Pipeline Architecture</h2>
<p>The evaluation infrastructure that runs this in practice looks like this:</p>
<pre tabindex="0"><code>Code / Prompt / Tool change
         │
         ▼
┌─────────────────────────────────┐
│      Pre-merge evaluation       │
│  Golden dataset (capability +   │
│  trajectory + response quality) │
│  Pass/fail gate                 │
└──────────────┬──────────────────┘
               │ passes
               ▼
┌─────────────────────────────────┐
│       Staging evaluation        │
│  Full workload replay on        │
│  recent production traffic      │
│  sample. LLM-as-judge scoring.  │
└──────────────┬──────────────────┘
               │ passes
               ▼
┌─────────────────────────────────┐
│     Canary deployment           │
│  Live traffic slice (5–10%).    │
│  Real-time quality metrics.     │
│  Human review sampling.         │
│  Automatic rollback on          │
│  quality threshold breach.      │
└──────────────┬──────────────────┘
               │ stable
               ▼
         Full production
</code></pre><p>Each stage narrows the blast radius of a failure. Pre-merge catches regressions before they&rsquo;re reviewed. Staging catches distribution shift that the golden dataset missed. Canary limits exposure before full rollout and provides automatic rollback if metrics degrade.</p>
<p>The canary stage is where real-time quality monitoring matters most. The metrics to watch:</p>
<ul>
<li><strong>Task completion rate</strong>: Did the agent finish the task, or did it abort, time out, or loop?</li>
<li><strong>Tool call efficiency</strong>: Average calls per task vs. baseline. A sudden increase signals trajectory degradation.</li>
<li><strong>Response quality score</strong>: LLM-judge scores on a rolling window. Set alert thresholds, not just manual checks.</li>
<li><strong>Latency p50/p95</strong>: Trajectory failures show up as latency spikes before they show up as quality failures.</li>
<li><strong>Cost per task</strong>: Unnecessary tool calls inflate cost before they necessarily degrade output.</li>
</ul>
<p>Alerts on these metrics are your canary in the canary deployment. By the time a quality failure is visible in user feedback, it&rsquo;s been running for hours. Metric alerts catch it in minutes.</p>
<hr>
<h2 id="the-feedback-loop">The Feedback Loop</h2>
<p>The evaluation infrastructure described above is a gate. The feedback loop is what turns it into a compounding advantage.</p>
<p>The loop has four steps:</p>
<ol>
<li><strong>Production monitoring surfaces a failure</strong> — anomaly in quality metrics, user report, sampling catch.</li>
<li><strong>Root cause analysis</strong> — trace the failure back to a specific decision in the execution path. Was it a tool call? A reasoning step? A context window overflow? The trace you captured during execution is what makes this fast.</li>
<li><strong>Golden dataset update</strong> — add the failing case as a new annotated example. Confirm the evaluation pipeline catches it.</li>
<li><strong>Fix and validate</strong> — implement the fix, verify it passes the new test, ship.</li>
</ol>
<p>Each cycle through the loop tightens the evaluation coverage and reduces the probability of the same failure class recurring. After six months of consistently running this process, teams typically have evaluation coverage that would have caught the majority of the failures they experienced in the first month.</p>
<p>The metric that matters here is time-to-detect: how long between when a quality regression is introduced and when the evaluation pipeline catches it. Teams without a feedback loop measure this in weeks. Teams with a mature loop measure it in minutes — at the pre-merge stage, before the regression is ever deployed.</p>
<hr>
<h2 id="the-evaluation-anti-patterns">The Evaluation Anti-Patterns</h2>
<p>A few patterns that look like evaluation infrastructure but don&rsquo;t actually protect you:</p>
<p><strong>Output-only evaluation with no trajectory checks.</strong> This misses tool misuse, reasoning drift, and efficiency failures entirely. You&rsquo;re testing whether the agent eventually reaches a good answer, not whether it&rsquo;s reliable.</p>
<p><strong>A golden dataset that never grows.</strong> A static dataset becomes less representative over time as your production distribution shifts. If you haven&rsquo;t added examples in three months, your coverage is drifting from your actual workload.</p>
<p><strong>LLM-as-judge without calibration.</strong> An uncalibrated judge may have systematic biases (length preference, confidence preference) that cause it to pass outputs it should fail. Calibrate against a human-rated set and audit judge outputs regularly.</p>
<p><strong>Evaluation that only runs before production.</strong> Without continuous quality monitoring in production, you have no signal on silent degradation. Pre-deployment gates catch regressions introduced by changes; continuous monitoring catches degradation caused by input distribution shift, external API behavior changes, or model serving infrastructure issues.</p>
<p><strong>No defined rollback criteria.</strong> If you don&rsquo;t know what metric threshold triggers a rollback before you deploy, you&rsquo;ll make that decision under pressure after something has already gone wrong.</p>
<hr>
<h2 id="what-to-build-first">What to Build First</h2>
<p>If you&rsquo;re starting from zero, the order matters:</p>
<p><strong>First</strong>: Instrument your agent to log structured traces — every tool call, every reasoning step, latency, cost. You can&rsquo;t evaluate what you can&rsquo;t observe, and you can&rsquo;t root cause failures without traces. This takes a day or two and unblocks everything else.</p>
<p><strong>Second</strong>: Build a golden dataset of 50–100 examples with output and trajectory annotations. Do this before the first production deployment, not after. Without a baseline, you have no way to know when quality starts drifting.</p>
<p><strong>Third</strong>: Wire the golden dataset into your CI/CD pipeline. Every pull request runs the evaluation suite; failing the suite blocks the merge. This is the gate that prevents regressions from shipping.</p>
<p><strong>Fourth</strong>: Add a staging replay stage that runs recent production traffic samples through the updated agent before full deployment. This catches distribution shift that your golden dataset doesn&rsquo;t cover.</p>
<p><strong>Fifth</strong>: Add production monitoring with alert thresholds on the key quality metrics. Canary deployments with automatic rollback on threshold breach.</p>
<p>The feedback loop — converting production failures into golden dataset entries — starts running from day one. It&rsquo;s a process, not a system. The discipline of doing it consistently is what compounds.</p>
<hr>
<h2 id="the-cost-of-skipping-this">The Cost of Skipping This</h2>
<p>Teams that skip evaluation infrastructure usually discover its value the same way: a production failure that takes longer to diagnose than it should, because there are no traces to follow and no baseline to compare against.</p>
<p>The cost isn&rsquo;t just the incident itself. It&rsquo;s the manual investigation that could have been automated. It&rsquo;s the regression that ships twice because the first fix didn&rsquo;t have a test. It&rsquo;s the model upgrade that degrades quality in a way nobody catches for three weeks because there&rsquo;s no continuous monitoring.</p>
<p>Evaluation infrastructure for agents is not overhead. It&rsquo;s the mechanism that makes deployment confidence possible — and deployment confidence is what separates teams that can iterate in hours from teams that spend weeks manually validating changes before they&rsquo;ll ship.</p>
<p><em>Part of the <a href="/series/ai-agents-in-production/">AI Agents in Production</a> series.</em></p>
]]></content:encoded></item><item><title>How Memory Works in AI Agents: Turning Stateless LLMs into Persistent, Learning Systems</title><link>https://blog.yottadynamics.com/posts/how-memory-works-in-ai-agents/</link><pubDate>Fri, 17 Apr 2026 09:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/how-memory-works-in-ai-agents/</guid><description>LLMs are stateless by design. Memory is the external infrastructure that turns them into agents that learn from experience, remember preferences, and actually improve over time. Here&amp;rsquo;s how it works — technically.</description><category>ai-agents</category><category>architecture</category><category>memory</category><category>vector-databases</category><category>ai-infrastructure</category><enclosure url="https://blog.yottadynamics.com/images/posts/how-memory-works-in-ai-agents.png" type="image/svg+xml"/><content:encoded><![CDATA[<p>AI agents are stateless control loops built around Large Language Models. That&rsquo;s not an opinion — it&rsquo;s the architecture. Once an API call ends, the model retains nothing. Every new prompt starts from zero.</p>
<p>This isn&rsquo;t a bug you can patch away with a better model or a bigger context window. It&rsquo;s structural. And it&rsquo;s why memory — implemented as external infrastructure, not model internals — is one of the most important <a href="/posts/ai-agent-architecture-memory-tools-orchestration/">architectural decisions</a> you&rsquo;ll make when building production agents.</p>
<hr>
<h2 id="why-context-windows-arent-memory">Why Context Windows Aren&rsquo;t Memory</h2>
<p>The obvious workaround is to stuff everything into the context window and keep growing it. This fails in practice for four reasons:</p>
<p><strong>Attention dilution.</strong> Transformers don&rsquo;t prioritize tokens — they give roughly equal weight to everything. A 200k-token window doesn&rsquo;t mean the model pays close attention to all 200k tokens. It means the relevant signal is competing with everything else.</p>
<p><strong>The &ldquo;lost in the middle&rdquo; problem.</strong> Information buried in the middle of a long context consistently receives less attention than content at the beginning or end. This is documented, reproducible, and gets worse as context grows.</p>
<p><strong>Linear cost scaling.</strong> Quadratic attention mechanisms mean doubling context roughly quadruples compute. For long-running or high-volume agents, this destroys unit economics fast.</p>
<p><strong>Session boundaries.</strong> Even a 2M-token window resets when the session ends. Everything disappears. The agent that spent three hours on a task yesterday knows nothing about it today.</p>
<p><a href="/posts/deep-dive-retrieval-augmented-generation-rag/">RAG</a> helps you ground responses in external facts but doesn&rsquo;t solve the continuity problem. It retrieves documents, not memories. It has no concept of user history, prior decisions, or accumulated preferences. True agent memory requires a different layer entirely.</p>
<hr>
<h2 id="the-four-memory-types">The Four Memory Types</h2>
<p>The CoALA cognitive architecture paper (Princeton, 2023) provides the clearest taxonomy for agent memory. Four layers, each with distinct storage and retrieval mechanics:</p>
<h3 id="short-term-working-memory">Short-Term (Working) Memory</h3>
<p>The in-context scratch pad. Managed within the LLM&rsquo;s active context window — recent messages, tool outputs, intermediate reasoning traces, retrieved data from this session.</p>
<p><strong>How it works</strong>: Conversation buffers with automatic summarization (typically hierarchical map-reduce) to compress history before it overflows the window. KV caching allows efficient extension without full recomputation.</p>
<p><strong>Limitation</strong>: Finite capacity and positional encoding decay. The agent can&rsquo;t hold a full project history in working memory — it can only hold a compressed, recency-weighted slice.</p>
<p><strong>Example</strong>: A deployment agent knows &ldquo;we&rsquo;re rolling out to staging first&rdquo; within the current session. When the session ends, that&rsquo;s gone unless something persists it.</p>
<h3 id="episodic-memory">Episodic Memory</h3>
<p>Timestamped, immutable records of specific past events. Think of it as the agent&rsquo;s diary — <em>what happened, when, and what the outcome was</em>.</p>
<p>Stored as metadata-enriched logs in vector or graph databases, often with temporal edges for sequence reconstruction. An episodic entry might be: &ldquo;On 2026-04-10, the agent deployed a patch but skipped the database migration step, causing a rollback.&rdquo;</p>
<p>This layer is what allows an agent to learn from specific failures rather than repeating them.</p>
<h3 id="semantic-memory">Semantic Memory</h3>
<p>Persistent, generalized knowledge — facts about users, domains, and systems that don&rsquo;t belong to a specific event. &ldquo;This user prefers Python over Java.&rdquo; &ldquo;Service X fails under load above 500 RPS.&rdquo; &ldquo;The staging environment requires VPN.&rdquo;</p>
<p>Stored as high-dimensional vector embeddings (128–2048 dimensions) for fast similarity search. This is the layer most similar to traditional RAG, but scoped to agent-specific learned knowledge rather than a document corpus.</p>
<h3 id="procedural-memory">Procedural Memory</h3>
<p>Encoded knowledge of <em>how</em> to do things: system prompts, decision trees, workflow templates, tool definitions, and fine-tuned behaviors. This layer doesn&rsquo;t retrieve facts — it shapes the agent&rsquo;s behavior at execution time.</p>
<p>Stored as JSON schemas, prompt templates, or lightweight LoRA adapters loaded dynamically. Changing procedural memory changes how the agent reasons, not just what it knows.</p>
<hr>
<h2 id="the-writemanageread-pipeline">The Write–Manage–Read Pipeline</h2>
<p>Memory isn&rsquo;t a database you append to. Effective implementations follow a four-stage pipeline that treats memory as a living structure — not an archive.</p>
<h3 id="1-extraction-and-encoding-write">1. Extraction and Encoding (Write)</h3>
<p>When a conversation or action completes, an extraction pipeline — typically LLM-driven — identifies what&rsquo;s worth keeping. Raw text gets chunked semantically (with overlap to preserve cross-chunk context), converted to dense vector embeddings, and enriched with metadata: timestamps, importance scores, entities extracted via NER.</p>
<p>The extraction layer makes one of four decisions for each candidate:</p>
<ul>
<li><strong>ADD</strong> — new fact, store it</li>
<li><strong>UPDATE</strong> — modify an existing record</li>
<li><strong>DELETE</strong> — remove outdated or contradicted information</li>
<li><strong>SKIP</strong> — duplicate or low-value, ignore</li>
</ul>
<p>This prevents unbounded growth and the noise that comes with it.</p>
<h3 id="2-storage-the-persistence-layer">2. Storage (The Persistence Layer)</h3>
<p>A production memory store is typically a hybrid architecture:</p>
<p><strong>Vector databases</strong> (Pinecone, Weaviate, ChromaDB, pgvector) handle semantic and episodic memory. HNSW indexes enable fast approximate nearest-neighbor search via cosine similarity or Euclidean distance. Choose based on your scale and whether you need managed infrastructure or tight database integration.</p>
<p><strong>Knowledge graphs</strong> handle relational memory — entities, relationships, bi-temporal modeling (event time vs. system time) for complex multi-hop queries. Useful when you need to reason about <em>how</em> facts relate, not just retrieve similar ones.</p>
<p><strong>Relational/key-value stores</strong> (PostgreSQL, Redis) handle structured state that requires ACID guarantees: session IDs, account state, workflow progress. Don&rsquo;t use vectors for this.</p>
<p>Converged databases that unify all three in one engine are available but add operational complexity. Start with the simplest layer that works, then add.</p>
<h3 id="3-management-consolidation-decay-forgetting">3. Management (Consolidation, Decay, Forgetting)</h3>
<p>This is the layer most implementations skip, and it&rsquo;s where memory systems rot.</p>
<p><strong>Summarization and compaction</strong>: Hierarchical map-reduce condenses long histories to manage token costs and context rot. Without this, retrieval quality degrades as the store grows.</p>
<p><strong>Reflection</strong>: Self-critique loops run on stored episodes to extract higher-level insights. An agent that failed three times on the same task type should generate a structured reflection: <em>what pattern caused the failure, what would prevent it</em>. These reflections are stored back as semantic memory.</p>
<p><strong>Consolidation and decay</strong>: Similar memories merge via cosine clustering (typical threshold: &gt;0.85). A temporal decay function reduces importance scores for older memories: <code>importance = relevance × exp(-λ × age)</code>. Stale or contradicted memories get pruned. Without decay, the store fills with outdated state that poisons retrieval.</p>
<p><strong>Trust scoring</strong>: Memories from different sources get different confidence weights. A fact the agent observed directly is more reliable than one inferred from a single user message.</p>
<h3 id="4-retrieval-read">4. Retrieval (Read)</h3>
<p>Before the LLM processes a new query, the memory layer runs a retrieval pass:</p>
<pre tabindex="0"><code>query → embedding → ANN search (top-k = 5–20)
                 ↓
         temporal filter
                 ↓
         graph traversal (multi-hop for relational queries)
                 ↓
         keyword search (BM25 hybrid)
                 ↓
         MMR re-ranking (diversity filter)
                 ↓
         inject into system prompt
</code></pre><p>Maximal Marginal Relevance (MMR) prevents the top-k results from being variations of the same memory. Retrieved items are re-ranked, summarized if needed, and injected into the system prompt with markers that ground the model&rsquo;s response in factual history rather than hallucinated plausibility.</p>
<p>The full loop:</p>
<pre tabindex="0"><code>Observe → Remember → Act → Reflect → Update
</code></pre><p>On modern hardware this runs in well under a second for typical store sizes. Latency only becomes a concern above tens of millions of embeddings without proper indexing and quantization.</p>
<hr>
<h2 id="the-failure-modes-to-watch">The Failure Modes to Watch</h2>
<p>A few that hit production systems regularly:</p>
<p><strong>Context window vs. retrieval mismatch.</strong> Short-term memory fills faster than expected, triggering premature summarization that loses details still needed. Tune buffer size and summarization thresholds against your actual usage patterns, not theoretical maximums.</p>
<p><strong>Retrieval surface contamination.</strong> If the extraction pipeline is too permissive, low-signal memories dilute retrieval quality. An agent that stores every utterance will retrieve noise. Importance scoring at write time is not optional.</p>
<p><strong>Stale memory poisoning.</strong> Without decay and consolidation, an agent will retrieve memories about a system&rsquo;s state from months ago as if they&rsquo;re current. Add timestamps, enforce TTLs for high-volatility facts, and run consolidation passes regularly.</p>
<p><strong>Privacy and multi-tenancy.</strong> User-scoped memory stores require strict isolation. Row-level security, namespace partitioning, and GDPR-compliant deletion are non-negotiable for production deployments. Plan for this from the start — retrofitting isolation into a shared store is painful.</p>
<p><strong>Evaluation gaps.</strong> Most teams measure agent task performance and <a href="/posts/observability-for-ai-agents/">notice memory failures only when things break</a>. Add explicit memory benchmarks: precision@K for retrieval accuracy, coherence testing under session resets, and latency under load for the retrieval path.</p>
<hr>
<h2 id="what-to-build-first">What to Build First</h2>
<p>If you&rsquo;re adding memory to an existing agent, do it in layers:</p>
<ol>
<li>
<p><strong>Start with a conversation buffer + hybrid vector/relational store.</strong> Embedding retrieval and a simple summarization pipeline gets you 80% of the value with 20% of the complexity.</p>
</li>
<li>
<p><strong>Add an extraction pipeline.</strong> LLM-driven extraction with ADD/UPDATE/DELETE/SKIP logic prevents store rot from day one.</p>
</li>
<li>
<p><strong>Add reflection prompts on completed tasks.</strong> Even simple self-critique (&ldquo;what worked, what didn&rsquo;t&rdquo;) fed back as semantic memory meaningfully improves subsequent performance.</p>
</li>
<li>
<p><strong>Add decay and consolidation.</strong> Schedule this as a background job. It doesn&rsquo;t need to run in the hot path.</p>
</li>
<li>
<p><strong>Give the agent memory tools.</strong> Let it decide what to remember and forget. Agentic memory management is more accurate than purely automatic pipelines because the model has context about what actually mattered.</p>
</li>
</ol>
<hr>
<h2 id="where-this-is-going">Where This Is Going</h2>
<p>As of 2026, memory has moved from &ldquo;nice to have&rdquo; to a first-class architectural component in serious agent deployments. A few trends worth tracking:</p>
<p><strong>Sleep-time computation.</strong> Agents that reorganize and consolidate memories during idle periods — offline, away from the hot path — are showing meaningful accuracy gains (18%+ in some published benchmarks) with lower runtime costs.</p>
<p><strong>Multi-agent shared memory.</strong> Distributed stores with consensus mechanisms let agent swarms share episodic and semantic memory without duplicating state. The coordination overhead is real, but so is the gain for long-running multi-agent workflows.</p>
<p><strong>Lifelong learning without catastrophic forgetting.</strong> This remains a hard problem. Continuous memory updates without forgetting earlier, still-valid knowledge requires careful consolidation strategy. No off-the-shelf solution solves this cleanly yet.</p>
<p>The next meaningful step is proactive, anticipatory memory — stores that surface relevant context <em>before</em> it&rsquo;s requested, based on what the agent is likely to need next. This moves memory from retrieval infrastructure to something closer to genuine cognitive architecture.</p>
<hr>
<p>Memory is what separates an expensive chatbot from an agent that actually improves over time. The infrastructure is mature and implementable today. The hard part is the design: what to store, when to forget, how to retrieve without adding noise. Get that right and everything downstream — reliability, personalization, task performance — improves with it.</p>
<p><em>Part of the <a href="/series/ai-agents-in-production/">AI Agents in Production</a> series.</em></p>
]]></content:encoded></item><item><title>A Deep Dive into Retrieval-Augmented Generation (RAG)</title><link>https://blog.yottadynamics.com/posts/deep-dive-retrieval-augmented-generation-rag/</link><pubDate>Fri, 10 Apr 2026 09:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/deep-dive-retrieval-augmented-generation-rag/</guid><description>RAG fixes the core problems with pure LLMs — hallucination, stale knowledge, private data — by making retrieval a first-class citizen. Here&amp;rsquo;s the full technical picture: vector search, hybrid retrieval, GraphRAG, agentic patterns, and what a production stack actually looks like in 2026.</description><category>rag</category><category>ai-infrastructure</category><category>architecture</category><category>vector-databases</category><category>llm</category><enclosure url="https://blog.yottadynamics.com/images/posts/deep-dive-retrieval-augmented-generation-rag.png" type="image/svg+xml"/><content:encoded><![CDATA[<p>In 2026, LLMs like Grok, GPT-4o, Claude, and Llama 4 power everything from customer support bots to research assistants. But pure generative AI still hallucinates facts, works with outdated knowledge, and has no access to your private data. <strong>Retrieval-Augmented Generation (RAG)</strong> fixes all three by making retrieval a first-class architectural concern — not a workaround, but the standard pattern behind every production-grade AI application worth operating today.</p>
<hr>
<h2 id="why-pure-generative-ai-falls-short">Why Pure Generative AI Falls Short</h2>
<p>Traditional LLMs are trained on a massive but <em>static</em> snapshot of data. Once training ends, four structural limitations kick in:</p>
<p><strong>No real-time knowledge.</strong> The model&rsquo;s world froze at its training cutoff. Ask it about something that happened last month and it either doesn&rsquo;t know or — worse — confidently makes something up.</p>
<p><strong>No access to private data.</strong> Your internal runbooks, Slack history, architecture decision records, and customer contracts weren&rsquo;t in the training set. The model can&rsquo;t retrieve them.</p>
<p><strong>Hallucination.</strong> When an LLM doesn&rsquo;t know something, it doesn&rsquo;t say &ldquo;I don&rsquo;t know&rdquo; — it generates a plausible-sounding answer. This is a feature of how transformers work, not a bug that gets patched away with a better model.</p>
<p><strong>Context window limits.</strong> You can stuff documents into a prompt, but attention is expensive. Long documents get truncated or noisily summarized, losing critical details buried in the middle.</p>
<p>RAG addresses all four by giving the LLM a <a href="/posts/how-memory-works-in-ai-agents/">dynamic external memory</a> it can query on demand.</p>
<hr>
<h2 id="what-is-rag">What Is RAG?</h2>
<p>The core idea in one sentence: <strong>retrieve relevant documents → augment the prompt with them → generate a grounded answer.</strong></p>
<p>Introduced in the 2020 paper <em>&ldquo;Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks&rdquo;</em> by Lewis et al. (Facebook AI Research), the pattern has since become ubiquitous. Every major framework — LangChain, LlamaIndex, Haystack — and every major vector database — Pinecone, Weaviate, Qdrant, pgvector — treats RAG as the default workflow.</p>
<p>The key insight is simple: instead of hoping the model memorized the right fact during training, you find the fact at query time and hand it to the model as context. You turn a closed-book exam into an open-book exam.</p>
<hr>
<h2 id="how-rag-works-the-full-pipeline">How RAG Works: The Full Pipeline</h2>
<p>RAG operates in two phases. Offline indexing runs once (and is updated incrementally). Online querying runs on every user request.</p>
<h3 id="phase-1--offline-indexing">Phase 1 — Offline Indexing</h3>
<p><strong>Data ingestion.</strong> Pull from every source that holds knowledge your users need: PDFs, Confluence pages, Notion databases, Slack exports, Git repositories, SQL tables, REST APIs. Format conversion (HTML parsing, PDF extraction) happens here, alongside deduplication and access-control tagging.</p>
<p><strong>Chunking.</strong> Raw documents are split into smaller pieces before embedding. This step is chronically underestimated. The chunking strategy shapes retrieval quality more than most people realize:</p>
<ul>
<li><em>Fixed-size</em> splitting (every 512 tokens) is simple but crude — it breaks sentences mid-thought.</li>
<li><em>Semantic chunking</em> uses embeddings to detect topic boundaries, then splits there. Better relevance, more compute at index time.</li>
<li><em>Hierarchical / parent-child</em> chunking indexes small chunks for retrieval precision, but stores pointers back to the full parent section for generation context. This is the pattern used in most production systems today.</li>
</ul>
<p>A 512–1,024 token chunk size with 10–20% overlap is a reasonable starting point. Overlap prevents information at chunk boundaries from falling into the gap between two chunks.</p>
<p><strong>Embedding.</strong> Each chunk is converted to a high-dimensional dense vector by an embedding model. The vector captures <em>semantic meaning</em>, not keywords — so &ldquo;account recovery&rdquo; and &ldquo;password reset&rdquo; land near each other in vector space because the model has learned they refer to the same concept.</p>
<p>The embedding model is the most consequential single choice in your RAG stack. The same model must be used during both indexing and query time — mixing models breaks semantic similarity entirely.</p>
<p>Popular choices in 2026: OpenAI <code>text-embedding-3-large</code> (3,072 dimensions), Voyage AI <code>voyage-3-large</code> (strong on long documents), <code>Snowflake-arctic-embed</code> (best open-source general-purpose), <code>nomic-embed-text</code> (fully open, excellent for on-prem).</p>
<p><strong>Vector storage.</strong> Embeddings and metadata (source file, timestamp, page number, access level) are loaded into a vector database that supports fast approximate nearest-neighbor (ANN) search. In 2026, the dominant production options are Pinecone Serverless (zero ops), Weaviate (rich filtering, hybrid search built-in), and Qdrant (Rust-based, extremely fast). For teams already running PostgreSQL, <code>pgvector</code> is a pragmatic starting point before scale forces a migration.</p>
<h3 id="phase-2--online-querying">Phase 2 — Online Querying</h3>
<p><strong>Query embedding.</strong> The user&rsquo;s question is embedded with the same model used during indexing. The result is the search key.</p>
<p><strong>Retrieval.</strong> The vector database performs an ANN search — typically cosine similarity or inner product — and returns the top-k most semantically similar chunks. k=5–20 is the typical range. More chunks give the LLM more context; fewer improve signal quality and reduce token cost.</p>
<p><strong>Re-ranking (optional but recommended).</strong> A cross-encoder model re-scores the top-k candidates by attending jointly to the query and each candidate chunk. Cross-encoders are slower than ANN but dramatically improve relevance ordering. Good options: <code>bge-reranker-v2</code>, Cohere Rerank 3, Jina Reranker. Run re-ranking on the top-20 ANN results and pass the top-5 survivors to the LLM.</p>
<p><strong>Prompt augmentation.</strong> Retrieved chunks are injected into the prompt alongside strict grounding instructions:</p>
<pre tabindex="0"><code>You are a technical assistant. Answer the question using ONLY the context
provided below. If the answer is not in the context, say so explicitly.
Cite the source document for every claim.

[Context]
[CHUNK 1 — Source: k8s-runbook.pdf, p.3]
...
[CHUNK 2 — Source: incident-log-2025-11-02.md]
...

Question: {user_question}
</code></pre><p><strong>Generation.</strong> The augmented prompt goes to the LLM. With grounded context, hallucination rates drop sharply. The model is constrained to what&rsquo;s in the retrieved chunks rather than what&rsquo;s in its parametric memory.</p>
<p><strong>Post-processing.</strong> Citation links, source attribution, confidence scoring, or response verification may be applied before the answer is returned.</p>
<p>Here&rsquo;s the full pipeline — offline indexing and graph construction on top, online query path below:</p>
<div style="overflow-x: auto; margin: 2rem 0; padding: 2rem 1.5rem; background: #07101E; border-radius: 12px; border: 1px solid #1E2A42;">
  <div class="mermaid" style="min-width: 600px;">

%%{init: {'themeVariables': {'fontSize': '13px'}, 'flowchart': {'nodeSpacing': 35, 'rankSpacing': 45}}}%%
flowchart TD
    subgraph INDEX["Main Indexing Pipeline"]
        direction LR
        SRC["📄 Sources\nPDFs · APIs · DBs"]
        CHK["✂️ Chunking\n512–1024 tokens\n10–20% overlap"]
        EMB["🔢 Embedding\nDense vectors"]
        VDB["🗄️ Vector Store\nANN index"]

        SRC --> CHK --> EMB --> VDB
    end

    subgraph GRAPH["Graph Pipeline"]
        direction LR
        EXT["🔍 Entity Extraction\nEntities + relations"]
        COM["🕸️ Community Detection\nLeiden clustering"]
        KG["📊 Knowledge Graph\nNeo4j / Memgraph"]

        EXT --> COM --> KG
    end

    SRC -.->|"parallel\ningestion"| EXT
    KG -.->|"nodes carry\nembeddings"| VDB

    style INDEX fill:#141C2E,stroke:#00D4FF,color:#E8EDF5
    style GRAPH fill:#141C2E,stroke:#00A8C8,color:#E8EDF5

    style SRC fill:#1E2A42,stroke:#8B9BB4,color:#E8EDF5
    style CHK fill:#1E2A42,stroke:#8B9BB4,color:#E8EDF5
    style EMB fill:#1A2A4A,stroke:#00D4FF,color:#00D4FF
    style VDB fill:#1A2A4A,stroke:#00D4FF,color:#00D4FF

    style EXT fill:#0F2A2A,stroke:#00A8C8,color:#00A8C8
    style COM fill:#0F2A2A,stroke:#00A8C8,color:#00A8C8
    style KG fill:#0F2A2A,stroke:#00A8C8,color:#00A8C8

  </div>
</div>

<p>And the online query path, including the CRAG feedback loop:</p>
<div style="overflow-x: auto; margin: 2rem 0; padding: 2rem 1.5rem; background: #07101E; border-radius: 12px; border: 1px solid #1E2A42;">
  <div class="mermaid" style="min-width: 600px;">

%%{init: {'themeVariables': {'fontSize': '13px'}, 'flowchart': {'nodeSpacing': 35, 'rankSpacing': 45}}}%%
flowchart TD
    Q["💬 User Query\nNatural language"]
    QE["🔢 Embed Query\nSame model as index"]

    Q --> QE

    subgraph RETRIEVAL["Retrieval Layer"]
        direction LR
        ANN["🔵 ANN Search\nCosine sim · k=5–20"]
        GT["🟢 Graph Traversal\nCypher / GQL"]
    end

    QE -->|"semantic search"| ANN
    QE -.->|"entity lookup"| GT

    RR["🔀 Re-rank\nCross-encoder scoring"]
    ANN --> RR
    GT -.-> RR

    PA["📝 Augment Prompt\nInject top-k context"]
    RR --> PA

    LLM["🤖 LLM Generation\nClaude · GPT-4o · Llama 4"]
    PA --> LLM

    ANS["✅ Grounded Answer\nCited + source-attributed"]
    LLM --> ANS

    ANS -.->|"CRAG: low confidence\ntriggers re-retrieval"| Q

    style RETRIEVAL fill:#141C2E,stroke:#00D4FF,color:#E8EDF5

    style Q fill:#2A1A0A,stroke:#F5A623,color:#F5A623
    style QE fill:#1A2A4A,stroke:#00D4FF,color:#00D4FF
    style ANN fill:#1A2A4A,stroke:#00D4FF,color:#00D4FF
    style GT fill:#0F2A2A,stroke:#00A8C8,color:#00A8C8
    style RR fill:#1A1A3A,stroke:#8B5CF6,color:#C4B5FD
    style PA fill:#1E2A42,stroke:#8B9BB4,color:#E8EDF5
    style LLM fill:#2A1A1A,stroke:#F87171,color:#FCA5A5
    style ANS fill:#0A2A14,stroke:#00FF88,color:#00FF88

  </div>
</div>

<hr>
<h2 id="search-capabilities-making-retrieval-actually-smart">Search Capabilities: Making Retrieval Actually Smart</h2>
<p>The retrieval layer is where most RAG systems succeed or fail. Pure vector search is just the starting point.</p>
<h3 id="hybrid-search">Hybrid Search</h3>
<p>Pure vector search underperforms on exact terms: product names, error codes, acronyms like &ldquo;CAPI&rdquo; or &ldquo;GitOps,&rdquo; version numbers. These have no semantic neighborhood — they just are what they are.</p>
<p>Hybrid search combines vector (semantic) search with traditional keyword search (BM25 or TF-IDF) and merges results via Reciprocal Rank Fusion or a tunable alpha parameter. An alpha of 0.7 (70% semantic, 30% keyword) is a reasonable production default for technical documentation.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> langchain.retrievers <span style="color:#f92672">import</span> EnsembleRetriever
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> langchain_community.retrievers <span style="color:#f92672">import</span> BM25Retriever
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>hybrid_retriever <span style="color:#f92672">=</span> EnsembleRetriever(
</span></span><span style="display:flex;"><span>    retrievers<span style="color:#f92672">=</span>[vector_retriever, bm25_retriever],
</span></span><span style="display:flex;"><span>    weights<span style="color:#f92672">=</span>[<span style="color:#ae81ff">0.7</span>, <span style="color:#ae81ff">0.3</span>]
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>Hybrid search consistently outperforms pure vector search in enterprise settings. If you&rsquo;re running Naive RAG in production today, adding hybrid search is the highest-ROI optimization available.</p>
<h3 id="metadata-filtering">Metadata Filtering</h3>
<p>Vector search doesn&rsquo;t know about access control, time windows, or organizational structure. Metadata filters apply SQL-like conditions before or during ANN search:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Only retrieve documents from the Engineering department</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># updated after 2025-01-01, visible to this user&#39;s clearance level</span>
</span></span><span style="display:flex;"><span>filters <span style="color:#f92672">=</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;department&#34;</span>: <span style="color:#e6db74">&#34;Engineering&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;last_updated&#34;</span>: {<span style="color:#e6db74">&#34;$gte&#34;</span>: <span style="color:#e6db74">&#34;2025-01-01&#34;</span>},
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;access_level&#34;</span>: {<span style="color:#e6db74">&#34;$lte&#34;</span>: user<span style="color:#f92672">.</span>clearance_level}
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>This is non-negotiable in multi-tenant applications, compliance-sensitive environments, or any organization where different roles have different data permissions.</p>
<h3 id="advanced-techniques">Advanced Techniques</h3>
<p><strong>Parent-document retrieval.</strong> Index small child chunks (128 tokens) for retrieval precision, return the full parent section (512–1,024 tokens) to the LLM for generation context. Best of both worlds.</p>
<p><strong>Query rewriting.</strong> Use a small LLM to transform user queries into retrieval-optimized forms before searching. &ldquo;What&rsquo;s our PTO policy?&rdquo; becomes &ldquo;employee vacation policy 2026 HR handbook annual leave days.&rdquo; HyDE (Hypothetical Document Embeddings) takes this further: generate a hypothetical answer to the query, then embed <em>that answer</em> as the search key. Counterintuitive, but it dramatically improves recall on complex questions.</p>
<p><strong>Multi-vector / ColBERT.</strong> Rather than one embedding per chunk, ColBERT produces one embedding per token. Matching happens at the token level via MaxSim. Finer-grained than standard dense retrieval, at the cost of higher storage and compute.</p>
<hr>
<h2 id="graph-capabilities-the-next-evolution--graphrag">Graph Capabilities: The Next Evolution — GraphRAG</h2>
<p>Standard RAG handles lookup queries well. It struggles with relational reasoning.</p>
<p>Ask &ldquo;What does our runbook say about pod eviction?&rdquo; and RAG finds the answer. Ask &ldquo;Which of our financial clients have infrastructure in regulated industries, and who are the engineers on those accounts?&rdquo; and RAG returns semantically similar document chunks — which is not the same as traversing structured relationships across entity types.</p>
<p>GraphRAG, pioneered by Microsoft Research in 2024 and now standard in LlamaIndex 1.x and LangGraph, addresses this by building a knowledge graph alongside the vector store.</p>
<h3 id="how-graphrag-works">How GraphRAG Works</h3>
<p><strong>Entity and relation extraction.</strong> During offline indexing, every chunk is scanned by an LLM to extract typed entities (Person, Organization, Technology, Concept) and labeled relationships (FOUNDED, REPORTS_TO, USES, DEPLOYED_ON). The results are stored as a graph: nodes with properties, typed edges, and embedding vectors on each node.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> llama_index.core <span style="color:#f92672">import</span> KnowledgeGraphIndex
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> llama_index.graph_stores.neo4j <span style="color:#f92672">import</span> Neo4jGraphStore
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>kg_index <span style="color:#f92672">=</span> KnowledgeGraphIndex<span style="color:#f92672">.</span>from_documents(
</span></span><span style="display:flex;"><span>    documents,
</span></span><span style="display:flex;"><span>    storage_context<span style="color:#f92672">=</span>StorageContext<span style="color:#f92672">.</span>from_defaults(graph_store<span style="color:#f92672">=</span>graph_store),
</span></span><span style="display:flex;"><span>    max_triplets_per_chunk<span style="color:#f92672">=</span><span style="color:#ae81ff">10</span>,
</span></span><span style="display:flex;"><span>    include_embeddings<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>,
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p><strong>Community detection.</strong> Microsoft&rsquo;s approach runs the Leiden algorithm on the entity graph to identify clusters of tightly related concepts. An LLM generates a plain-language summary of each cluster. At query time, these community summaries answer broad &ldquo;global&rdquo; questions — &ldquo;Summarize our entire competitive positioning&rdquo; — without scanning thousands of individual chunks.</p>
<p><strong>Query-time traversal.</strong> For specific, targeted queries, GraphRAG traverses the graph using Cypher (Neo4j) or GQL, combining path constraints with vector similarity:</p>
<pre tabindex="0"><code class="language-cypher" data-lang="cypher">MATCH (e:Entity)-[:RELATED_TO*1..3]-(related)
WHERE e.name CONTAINS &#34;RAG&#34;
WITH related,
     vector.similarity.cosine(related.embedding, $query_embedding) AS score
RETURN related ORDER BY score DESC LIMIT 10
</code></pre><p>The combination of graph traversal (structural constraints) and vector similarity (semantic relevance) produces results that are both topically correct and relationally coherent.</p>
<h3 id="when-to-add-graphrag">When to Add GraphRAG</h3>
<p>Graph construction is expensive — LLM-based entity extraction at scale, plus graph database operational overhead. Reach for it when:</p>
<ul>
<li>Queries span multiple documents and require multi-hop reasoning (&ldquo;who reports to the person who owns this system?&rdquo;)</li>
<li>Your domain is inherently relational: org charts, legal entity structures, supply chains, citation networks</li>
<li>You need explainability — showing the exact graph path the AI followed is a powerful audit trail</li>
<li>Hallucination on relationship-heavy queries is causing real problems</li>
</ul>
<p>For simple lookup use cases, hybrid vector search is sufficient and far cheaper.</p>
<hr>
<h2 id="advanced-rag-patterns">Advanced RAG Patterns</h2>
<h3 id="agentic-rag">Agentic RAG</h3>
<p>In standard RAG, retrieval is a fixed step. In Agentic RAG, the LLM decides <em>at runtime</em> whether to retrieve, which retrieval strategy to use, whether to call external tools, and whether to iterate. This is implemented via LangGraph state machines or ReAct-style reasoning loops.</p>
<p>Agentic RAG enables multi-step workflows: decompose a complex question into sub-questions, retrieve context for each sub-question independently, synthesize across multiple retrieval steps, and produce a grounded final answer. This is <a href="/posts/ai-agent-architecture-memory-tools-orchestration/">the agent architecture</a> behind AI systems that feel genuinely intelligent rather than just &ldquo;fast search.&rdquo;</p>
<h3 id="corrective-rag-crag">Corrective RAG (CRAG)</h3>
<p>CRAG adds a verification step after retrieval. A lightweight judge LLM grades each retrieved chunk on a relevance score. Chunks below a threshold (typically 0.6) are discarded. If too few chunks survive, the system falls back to a web search rather than generating with insufficient context.</p>
<p>This is particularly valuable for production systems where retrieval quality degrades over time as the source documents drift — CRAG catches the degradation before it propagates into bad answers.</p>
<h3 id="self-rag">Self-RAG</h3>
<p>Self-RAG fine-tunes the LLM to emit special control tokens mid-generation that trigger retrieval, evaluate retrieved documents, and critique its own output. Produces highly accurate, source-grounded responses. Requires fine-tuning, so it&rsquo;s not suitable for off-the-shelf deployment — but it&rsquo;s the direction frontier research is moving.</p>
<h3 id="multimodal-rag">Multimodal RAG</h3>
<p>Text isn&rsquo;t the only modality worth indexing. Multimodal RAG embeds images (CLIP, SigLIP), audio (Whisper → text), video (frame-level embeddings or transcript-based), and tables (structure-aware chunking) alongside text. As LLMs gain native vision capabilities, the retrieval pipeline extends naturally to heterogeneous document collections.</p>
<hr>
<h2 id="the-2026-production-stack">The 2026 Production Stack</h2>
<p>A typical production RAG system in 2026:</p>
<table>
  <thead>
      <tr>
          <th>Layer</th>
          <th>Component</th>
          <th>Common Choices</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Orchestration</td>
          <td>Workflow engine</td>
          <td>LangGraph, LlamaIndex Workflows</td>
      </tr>
      <tr>
          <td>Embeddings</td>
          <td>Text embedding</td>
          <td>Voyage AI, OpenAI, Snowflake-arctic-embed</td>
      </tr>
      <tr>
          <td>Vector DB</td>
          <td>ANN search</td>
          <td>Pinecone, Weaviate, Qdrant</td>
      </tr>
      <tr>
          <td>Graph DB</td>
          <td>Knowledge graph</td>
          <td>Neo4j AuraDB, Memgraph</td>
      </tr>
      <tr>
          <td>Reranker</td>
          <td>Cross-encoder</td>
          <td>Cohere Rerank 3, BGE-Reranker-v2</td>
      </tr>
      <tr>
          <td>LLM</td>
          <td>Generation</td>
          <td>Claude 3.7, GPT-4o, Llama 4 405B</td>
      </tr>
      <tr>
          <td>Evaluation</td>
          <td>Quality metrics</td>
          <td>RAGAS, DeepEval, TruLens</td>
      </tr>
      <tr>
          <td>Observability</td>
          <td>Tracing</td>
          <td>LangSmith, Arize Phoenix</td>
      </tr>
  </tbody>
</table>
<hr>
<h2 id="evaluation-measuring-what-matters">Evaluation: Measuring What Matters</h2>
<p>A RAG system you can&rsquo;t measure is a RAG system you can&rsquo;t improve. The RAGAS framework provides the standard automated metrics:</p>
<p><strong>Faithfulness</strong> — does the answer contain only claims supported by the retrieved context? Target: &gt;0.90.</p>
<p><strong>Answer relevance</strong> — does the answer address the question that was actually asked? Target: &gt;0.85.</p>
<p><strong>Context precision</strong> — are the retrieved chunks relevant to the question? Target: &gt;0.80.</p>
<p><strong>Context recall</strong> — does the retrieved context contain all the information needed to answer? Target: &gt;0.75.</p>
<p>Automated metrics are necessary but not sufficient. Build a golden dataset of 100–200 representative queries with human-annotated reference answers. Run automated metrics on every pull request. Run human evaluation quarterly. <a href="/posts/observability-for-ai-agents/">Monitor for drift</a>: retrieval quality degrades silently as source documents change, and you need to catch it before your users do.</p>
<hr>
<h2 id="challenges-and-best-practices">Challenges and Best Practices</h2>
<p><strong>Retrieval quality is everything.</strong> The LLM can&rsquo;t generate a grounded answer from irrelevant context. Garbage in, garbage out — but the garbage is invisible because the LLM confidently uses whatever you give it. Invest disproportionately in the retrieval layer.</p>
<p><strong>Latency adds up fast.</strong> Query embedding (20–50ms) + ANN search (10–40ms) + re-ranking (100–300ms) + LLM generation (500–1,200ms) = a budget you can blow easily. Stream tokens to the UI, show retrieval results while the LLM is generating, and cache frequent query embeddings.</p>
<p><strong>Knowledge base freshness is a silent failure mode.</strong> Stale knowledge produces confidently wrong answers. Implement event-driven incremental indexing: trigger re-indexing via webhooks when source documents change in Confluence, Notion, or your CMS. Store a <code>last_updated</code> timestamp on every chunk so you can deprioritize stale content.</p>
<p><strong>Start simple.</strong> The progression that works: Naive RAG → add hybrid search → add re-ranking → add GraphRAG only when multi-hop reasoning is genuinely needed. Don&rsquo;t over-engineer the first version.</p>
<p><strong>Test adversarially.</strong> The queries that break your system are the ones users won&rsquo;t report — they&rsquo;ll just stop trusting the product. Probe with out-of-scope questions, questions that presuppose false information, and questions that require reasoning the retrieval pipeline can&rsquo;t support.</p>
<hr>
<h2 id="real-world-impact">Real-World Impact</h2>
<p>Companies operating sophisticated RAG and GraphRAG systems in 2026 consistently report:</p>
<ul>
<li>40–70% reductions in hallucination rates compared to bare LLM deployments</li>
<li>Significant gains in user trust, particularly in high-stakes domains (legal, financial, clinical)</li>
<li>Reduced context window costs — graph-structured retrieval surfaces precise, relevant context rather than stuffing large document sections into the prompt</li>
</ul>
<p>The use cases span every vertical: customer support bots that answer from the latest product docs, legal research assistants that traverse case law and internal memos, financial analysts querying earnings calls and regulatory filings, internal company &ldquo;second brains&rdquo; that understand org structure through graph relationships, and scientific research copilots that reason across citation graphs.</p>
<hr>
<h2 id="final-thoughts">Final Thoughts</h2>
<p>RAG didn&rsquo;t just improve AI — it changed what production AI means. By combining semantic vector search with knowledge graph reasoning and intelligent agents, you give models both breadth (massive unstructured knowledge) and depth (structured relational understanding).</p>
<p>The result is AI that is accurate, up-to-date, auditable, and useful at enterprise scale. The winning stack in 2026 isn&rsquo;t just vectors. It&rsquo;s <strong>vector search + knowledge graphs + intelligent agents</strong>. The systems that master this combination are the ones that feel truly intelligent.</p>
<p>Drop your specific use case in the comments — enterprise search, research, customer support — and we&rsquo;ll sketch a full architecture for you.</p>
]]></content:encoded></item><item><title>The Technical Blueprint for AI Speed: Markdown vs. RAG</title><link>https://blog.yottadynamics.com/posts/markdown-vs-rag-ai-storage-architecture/</link><pubDate>Tue, 07 Apr 2026 09:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/markdown-vs-rag-ai-storage-architecture/</guid><description>The storage format you choose for AI knowledge directly shapes your system&amp;rsquo;s latency, token density, and semantic clarity. A pragmatic breakdown of when to use raw Markdown, when to build a RAG pipeline, and why the best production systems use both.</description><category>ai-infrastructure</category><category>rag</category><category>architecture</category><category>platform-engineering</category><category>ai-agents</category><enclosure url="https://blog.yottadynamics.com/images/posts/markdown-vs-rag-ai-storage-architecture.png" type="image/svg+xml"/><content:encoded><![CDATA[<p>In the race to build high-performance AI infrastructure, the storage format you choose directly shapes your system&rsquo;s latency, token density, and semantic clarity. For AI engineers and system architects, the choice between raw Markdown storage and <a href="/posts/deep-dive-retrieval-augmented-generation-rag/">Retrieval-Augmented Generation (RAG)</a> isn&rsquo;t ideological — it&rsquo;s a pragmatic optimization problem driven by scale, workload, and performance constraints.</p>
<hr>
<h2 id="the-case-for-markdown-maximum-semantic-signal-with-minimal-overhead">The Case for Markdown: Maximum Semantic Signal with Minimal Overhead</h2>
<p>Markdown is far more than lightweight plain text. It serves as a clean, structured roadmap that Large Language Models (LLMs) parse efficiently.</p>
<h3 id="token-efficiency">Token Efficiency</h3>
<p>Markdown delivers approximately 95% of the semantic structure found in HTML while adding only about 5% token overhead from formatting. HTML, by contrast, often burdens the context window with 18% or more boilerplate tags, scripts, and styling noise. Real-world benchmarks show Markdown can reduce token consumption by 20–30% compared to HTML equivalents, lowering API costs and enabling denser, more valuable context within the same window.</p>
<h3 id="attention-pattern-optimization">Attention Pattern Optimization</h3>
<p>Transformer models develop stronger, more reliable attention patterns on consistently structured data. Markdown&rsquo;s predictable hierarchy (<code># H1</code>, <code>## H2</code>, lists, code blocks) helps models focus on semantically important elements. Studies and observations indicate models trained on Markdown-heavy datasets achieve roughly 15% better performance on structured reasoning tasks compared to mixed or noisier formats.</p>
<h3 id="zero-latency-retrieval-for-small-to-medium-datasets">Zero-Latency Retrieval for Small-to-Medium Datasets</h3>
<p>For knowledge bases under ~100MB — personal notes, project wikis, small documentation sets — direct Markdown ingestion is unbeatable. You skip the entire &ldquo;retrieval tax&rdquo;: no vector database queries, no embedding lookups, no reranking. The model receives the full, unfiltered file immediately. This delivers instant context and eliminates variability introduced by chunking or search approximations.</p>
<hr>
<h2 id="the-case-for-rag-scalability-with-consistent-sub-second-performance">The Case for RAG: Scalability with Consistent Sub-Second Performance</h2>
<p>When your data grows into gigabytes or terabytes, stuffing the entire context hits hard limits on latency, cost, and model comprehension.</p>
<h3 id="consistent-latency">Consistent Latency</h3>
<p>Modern high-performance RAG pipelines achieve vector search latencies around 30–50ms on optimized vector databases, with full retrieval — including hybrid search and reranking — often completing under 130ms on contemporary hardware. This predictability holds even as your archive scales massively.</p>
<h3 id="accuracy-lift-through-structure-aware-chunking">Accuracy Lift Through Structure-Aware Chunking</h3>
<p>Markdown&rsquo;s natural headers (H1–H6) enable content-aware chunking, where the system retrieves coherent logical sections rather than arbitrary sentence fragments. This approach can improve retrieval accuracy by 40–60% over naive fixed-size splitting, because chunks align with actual concepts and maintain contextual integrity.</p>
<h3 id="hybrid-search-advantage">Hybrid Search Advantage</h3>
<p>Markdown files pair perfectly with both semantic embeddings and classical keyword methods like BM25. Hybrid retrieval — combining dense vector similarity with sparse exact-term matching — delivers significantly more accurate results, often in the 40–60%+ improvement range for relevance in mixed query scenarios. This fusion captures both &ldquo;meaning&rdquo; and precise terminology that pure vector search might miss.</p>
<hr>
<h2 id="comparison-at-a-glance">Comparison at a Glance</h2>
<table>
  <thead>
      <tr>
          <th></th>
          <th>Markdown (Direct)</th>
          <th>RAG (Vector + Hybrid)</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Best for</strong></td>
          <td>Personal notes, small wikis, projects &lt; 100MB</td>
          <td>Enterprise knowledge bases, massive or dynamic datasets</td>
      </tr>
      <tr>
          <td><strong>Speed</strong></td>
          <td>Instant for small data; degrades sharply with overload</td>
          <td>Consistent sub-second retrieval (often &lt;130ms total) at scale</td>
      </tr>
      <tr>
          <td><strong>Accuracy</strong></td>
          <td>High — model sees complete, unfiltered files</td>
          <td>Variable but tunable; strong with good chunking and hybrid search</td>
      </tr>
      <tr>
          <td><strong>Setup</strong></td>
          <td>Minimal — save as <code>.md</code> and load</td>
          <td>Higher — requires embeddings, vector DB, chunking strategy</td>
      </tr>
      <tr>
          <td><strong>Token efficiency</strong></td>
          <td>Excellent (low overhead, high signal)</td>
          <td>Good, but depends on retrieved chunk quality</td>
      </tr>
      <tr>
          <td><strong>Debuggability</strong></td>
          <td>Trivial — <code>grep</code>, Git diffs</td>
          <td>Requires tracing retrieval paths</td>
      </tr>
  </tbody>
</table>
<hr>
<h2 id="the-hybrid-strategy-markdown-as-the-source-of-truth">The Hybrid Strategy: Markdown as the Source of Truth</h2>
<p>The most robust production architectures treat Markdown as the canonical source while layering RAG on top for scale.</p>
<h3 id="why-this-wins">Why This Wins</h3>
<p><strong>Debuggability.</strong> When the AI hallucinates or errs, you can instantly search your raw Markdown files with tools like <code>grep</code> or <code>ripgrep</code> to audit the underlying data. No opaque vector indices to reverse-engineer.</p>
<p><strong>Version control and auditability.</strong> Store your <a href="/posts/how-memory-works-in-ai-agents/">&ldquo;AI memory&rdquo;</a> in plain Markdown files under Git. Track every change to knowledge with full history, branches, and diffs — something binary vector stores or databases make cumbersome or impossible.</p>
<p><strong>Seamless transition.</strong> Start simple with direct Markdown loading for small sets. As data grows, add indexing and RAG without rewriting your content. Markdown&rsquo;s structure makes chunking, metadata extraction, and hybrid search far more effective.</p>
<p>In practice, many advanced systems index Markdown files directly for both BM25 keyword search and vector embeddings, preserving the format&rsquo;s strengths while gaining scalability.</p>
<hr>
<h2 id="final-takeaway-for-ai-architects">Final Takeaway for AI Architects</h2>
<p><strong>Choose Markdown-first when you can.</strong> It maximizes semantic density, minimizes latency and cost for moderate scales, and keeps your system transparent and maintainable.</p>
<p><strong>Layer on RAG (especially hybrid) when scale demands it.</strong> It provides predictable performance and handles massive, dynamic knowledge without overwhelming context windows.</p>
<p>The winning blueprint isn&rsquo;t Markdown <em>or</em> RAG — it&rsquo;s Markdown as the source of truth, with high-quality RAG as the scalable retrieval layer.</p>
<p>This combination delivers the best of both worlds: clean, efficient data representation for LLMs and engineered retrieval that scales without sacrificing clarity or debuggability. Implement this thoughtfully, measure your specific latency/accuracy/cost tradeoffs, and iterate. In AI infrastructure, the format you choose today determines how fast — and how reliably — your systems will run tomorrow.</p>
]]></content:encoded></item><item><title>AI Agent Architecture: Memory, Tools, Orchestration, and Production</title><link>https://blog.yottadynamics.com/posts/ai-agent-architecture-memory-tools-orchestration/</link><pubDate>Mon, 06 Apr 2026 10:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/ai-agent-architecture-memory-tools-orchestration/</guid><description>Most &amp;lsquo;my agent broke&amp;rsquo; investigations don&amp;rsquo;t end at the model. They end in memory design, tool scope, orchestration logic, or missing observability. This post covers the plumbing that actually determines whether an agent works in production.</description><category>ai-agents</category><category>architecture</category><category>operations</category><category>ai-infrastructure</category><category>observability</category><enclosure url="https://blog.yottadynamics.com/images/posts/ai-agent-architecture-memory-tools-orchestration.png" type="image/svg+xml"/><content:encoded><![CDATA[<p>In <a href="/posts/what-is-an-ai-agent-a-practical-guide/">Part 1</a> we covered what an AI agent is, how it differs from chatbots and copilots, and how to match autonomy level to the task. This post goes deeper — into the plumbing that actually determines whether an agent works in production.</p>
<p>Most &ldquo;my agent broke&rdquo; investigations don&rsquo;t end at the model. They end in <a href="/posts/how-memory-works-in-ai-agents/">memory design</a>, tool scope, orchestration logic, or <a href="/posts/observability-for-ai-agents/">missing observability</a>. That&rsquo;s what this post is about.</p>
<hr>
<h2 id="orchestration-patterns-the-architectures-that-drive-agent-behavior">Orchestration Patterns: The Architectures That Drive Agent Behavior</h2>
<p>Orchestration is not a single technique. It&rsquo;s a family of patterns — each with distinct trade-offs in reliability, cost, latency, and debuggability. Choosing the right one is a foundational architectural decision.</p>
<h3 id="react-reason--act">ReAct (Reason + Act)</h3>
<p>ReAct is the most widely deployed orchestration pattern. At each loop iteration, the model generates a thought (explicit reasoning about what to do next), selects an action (a tool call with parameters), observes the result, and updates its context before the next iteration.</p>
<pre tabindex="0"><code>┌──────────┐     ┌──────────┐     ┌─────────────┐
│  Thought │────▶│  Action  │────▶│ Observation │
│          │     │          │     │             │
│ Reason   │     │ Tool call│     │ Result +    │
│ about    │     │ with     │     │ context     │
│ next step│     │ params   │     │ update      │
└──────────┘     └──────────┘     └──────┬──────┘
      ▲                                  │
      └──────────────────────────────────┘
              repeat until terminal
</code></pre><p>The tight thought-action-observation cycle makes the model&rsquo;s reasoning auditable — you can trace exactly why it chose each tool call. This is its primary advantage for debugging.</p>
<p>The trade-off: ReAct is expensive. Every iteration requires a full model call. Long tasks accumulate latency and token cost linearly. It also assumes the model can plan one step ahead effectively. When tasks require reasoning ten iterations out, single-step reasoning degrades and the agent begins making locally reasonable but globally suboptimal decisions.</p>
<h3 id="chain-of-thought-cot-planning">Chain-of-Thought (CoT) Planning</h3>
<p>CoT separates planning from execution. The model produces an explicit multi-step plan before taking any action. Orchestration then executes that plan sequentially, feeding results back as observations.</p>
<pre tabindex="0"><code>┌─────────────────────────────────┐
│         Planning phase          │
│                                 │
│  Model generates full plan      │
│  before any tool is called      │
│                                 │
│  Step 1: fetch customer record  │
│  Step 2: check order history    │
│  Step 3: calculate refund       │
│  Step 4: send confirmation      │
└────────────────┬────────────────┘
                 │ optional: human review here
                 ▼
┌─────────────────────────────────┐
│         Execution phase         │
│                                 │
│  Orchestration executes steps   │
│  sequentially, feeds results    │
│  back as observations           │
└─────────────────────────────────┘
</code></pre><p>The advantage: planning upfront reduces model calls during execution, lowers cost, and creates a natural checkpoint for human review before any action fires.</p>
<p>The limitation: the upfront plan is static. If early tool calls return unexpected results, a pure CoT agent has no mechanism to revise mid-execution. In dynamic environments — where APIs fail, data is missing, or results differ from expectations — rigid plans break down. The fix is hybrid orchestration: plan upfront, but re-enter a ReAct loop whenever observations deviate significantly from plan assumptions.</p>
<h3 id="hierarchical-planning">Hierarchical Planning</h3>
<p>For complex, long-horizon tasks, flat orchestration breaks down. Hierarchical planning introduces two levels: a high-level planner that decomposes the goal into sub-goals, and sub-agents or lower-level orchestrators that execute each sub-goal independently.</p>
<pre tabindex="0"><code>┌─────────────────────────────────────────┐
│           High-level planner            │
│                                         │
│  Decomposes goal into sub-goals.        │
│  Operates over abstract objectives.     │
│  Does not track tool-level details.     │
└────────┬──────────────┬─────────────────┘
         │              │
         ▼              ▼
┌────────────┐    ┌────────────┐
│  Sub-agent │    │  Sub-agent │
│     A      │    │     B      │
│            │    │            │
│ Specialized│    │ Specialized│
│ tools and  │    │ tools and  │
│ context    │    │ context    │
└────────────┘    └────────────┘
         │              │
         └──────┬───────┘
                ▼
     Results composed back
     by the high-level planner
</code></pre><p>This separation keeps context windows lean at both levels — the planner doesn&rsquo;t need tool-level details, and sub-agents don&rsquo;t need full task context. The trade-off is coordination complexity. Debugging hierarchical systems requires distributed tracing that crosses agent boundaries. Failures can originate at any level and manifest at another.</p>
<h3 id="selecting-an-orchestration-pattern">Selecting an Orchestration Pattern</h3>
<pre tabindex="0"><code>┌──────────────────────────────────────────────────────────────────┐
│ Decision guide                                                   │
├──────────────────────────────────────────────────────────────────┤
│ Task has unpredictable step sequence       → ReAct              │
│                                                                  │
│ Task structure is known and stable,        → CoT with           │
│ or you need a human approval checkpoint      plan validation    │
│ before execution begins                                          │
│                                                                  │
│ Task exceeds reliable planning horizon     → Hierarchical       │
│ of a single model, or sub-tasks need         planning           │
│ domain-specific context that shouldn&#39;t                          │
│ bleed across a monolithic window                                 │
└──────────────────────────────────────────────────────────────────┘
</code></pre><p>In practice, most production systems are hybrids. The outer loop uses hierarchical decomposition; individual sub-tasks use ReAct; high-stakes sub-tasks inject a CoT plan-then-confirm step before execution.</p>
<hr>
<h2 id="memory-architecture-what-the-agent-knows-and-for-how-long">Memory Architecture: What the Agent Knows and for How Long</h2>
<p>Poor memory design is the leading cause of agent unreliability in production. The failure modes are subtle — the agent appears to work, then silently loses track of context, repeats completed steps, or contradicts earlier decisions. By the time the bug is visible, it&rsquo;s several iterations into a corrupted state.</p>
<p>The design question isn&rsquo;t &ldquo;Do I need memory?&rdquo; It&rsquo;s: what must survive across runs, what can be reconstructed cheaply, and what should never be stored?</p>
<h3 id="short-term--working-memory-the-context-window">Short-Term / Working Memory: The Context Window</h3>
<p>Working memory is the context window — the content assembled for the current model call. It holds the system prompt, current goal, active tool schemas, prior tool results, reasoning steps, and recent observations. It is fast, always available, and finite.</p>
<p>The architectural risk is <strong>context degradation</strong>: as the window fills over a long task, early content — including the original goal — competes with noise. The model doesn&rsquo;t forget in a hard, detectable way. It degrades softly: subtly worse decisions, goal drift, increased hallucination rates. You will not see an error. You&rsquo;ll see subtly wrong outputs.</p>
<pre tabindex="0"><code>Token usage across a session
─────────────────────────────────────────────
Iter   Context tokens   % of window
─────────────────────────────────────────────
1      2,260            14%   ░░░░░░░░░░
2      2,440            16%   ░░░░░░░░░░
3      4,890            31%   ███░░░░░░░
4      7,120            45%   ████░░░░░░
5      9,440            59%   █████░░░░░  ← alert threshold
6      3,200            20%   ██░░░░░░░░  ← summarization fired
7      5,100            32%   ███░░░░░░░
─────────────────────────────────────────────
Alert at 60%. Critical at 80%.
By 90%, degradation is already occurring.
</code></pre><p>Three mitigations address this:</p>
<p><strong>Periodic summarization</strong> compresses accumulated reasoning and observations into a dense summary that replaces the raw content. Summarize what happened and what was decided, but keep the most recent tool results verbatim — they are the most decision-relevant content.</p>
<p><strong>Smart eviction</strong> tracks which context elements are still decision-relevant and removes those that aren&rsquo;t. Tool results from five iterations ago rarely need to be verbatim if a summary captures their outcome. This requires tagging context elements at insertion time.</p>
<p><strong>Chunked execution</strong> breaks long tasks into sub-tasks, each with a clean context window. State is persisted externally between chunks and retrieved at the start of each. This is hierarchical orchestration applied to memory management.</p>
<p>Monitor token usage explicitly in your orchestration layer. Alert at 60% of the window limit — not 90%.</p>
<h3 id="long-term-memory-external-storage">Long-Term Memory: External Storage</h3>
<p>Long-term memory lives outside the context window and is retrieved on demand. It subdivides into three types that serve distinct purposes.</p>
<p><strong>Episodic memory</strong> stores logs of past agent runs — what was attempted, what succeeded, what failed, and why. This is the mechanism by which agents improve over time without retraining. When starting a new task, the agent retrieves relevant past episodes and uses them as few-shot context. Episodic memory is the foundation of self-improving agents and is chronically underimplemented.</p>
<p>Design decision: log goal, plan, key decision points, tool call summaries, and final outcome. Discard raw observation payloads after summarization. Full transcripts are expensive and noisy; final-outcome-only logging loses the reasoning that explains why outcomes occurred.</p>
<p><strong>Semantic memory</strong> stores factual knowledge and business data — the ground truth the agent needs to answer questions accurately. In production, this is implemented via <a href="/posts/deep-dive-retrieval-augmented-generation-rag/">RAG</a>: a vector store the agent queries via a tool, returning relevant chunks inserted into the context window on demand.</p>
<p>Naive top-k vector similarity retrieval works for simple factual lookups but degrades on complex queries. More robust approaches use hybrid retrieval (combining vector similarity with BM25 keyword search), query decomposition (breaking a complex query into sub-queries before retrieving), and re-ranking (using a second model to score retrieved chunks for relevance before insertion). Each adds latency; the question is whether retrieval quality justifies it for your use case.</p>
<p><strong>Procedural / Coordination memory</strong> is shared state in multi-agent systems — task queues, sub-task status, intermediate results, and cross-agent signals. This is the nervous system of multi-agent coordination.</p>
<h3 id="the-memory-design-principle">The Memory Design Principle</h3>
<pre tabindex="0"><code>For every category of information the agent needs, answer:
─────────────────────────────────────────────────────────
  What is its read/write frequency?
  How long must it survive?
  Who else needs access to it?
  What is the cost of losing it mid-task?
─────────────────────────────────────────────────────────
Wrong answers produce:
  Too much in context  →  high cost, latency, degradation
  Too little persisted →  broken workflow continuity
  Wrong things stored  →  security risk
</code></pre><hr>
<h2 id="tool-design-where-agent-reliability-is-won-or-lost">Tool Design: Where Agent Reliability Is Won or Lost</h2>
<p>The model decides which tools to call. The orchestration layer executes them. But the tools themselves determine whether those calls succeed reliably. Poorly designed tools are the second most common production failure mode — behind memory mismanagement — and the most fixable.</p>
<h3 id="the-three-tool-categories">The Three Tool Categories</h3>
<p><strong>Outbound API calls</strong> connect the agent to external systems: Slack, GitHub, internal microservices. Primary failure modes are parameter errors, transient network failures triggering retry logic that can produce duplicate actions, and authentication expiry mid-task.</p>
<p><strong>Custom functions</strong> are code you own, control, and test. They are deterministic and predictable. Use them whenever predictability matters more than flexibility — tax calculations, date arithmetic, schema validation, data transformations. Custom functions are the highest-reliability tool category and should be preferred for any computation that doesn&rsquo;t require external state.</p>
<p><strong>Data retrieval (RAG)</strong> grounds the agent in current facts rather than training data. Every domain-specific fact the agent needs to get right should have a retrieval path. If there is no tool that retrieves it, the agent will hallucinate it.</p>
<h3 id="tool-design-principles">Tool Design Principles</h3>
<p><strong>Single responsibility.</strong> A tool called <code>get_customer_data</code> that conditionally fetches orders, preferences, or account status depending on parameters is three tools disguised as one. Split it. Single-responsibility tools are easier to test, easier to mock, and dramatically easier to debug when they fail.</p>
<p><strong>Explicit, typed interfaces.</strong> Every parameter should have a type, a description, and — where applicable — a constraint on valid values. Design tool interfaces as if a careful junior engineer with no context will be calling them at 3 a.m. That junior engineer is the model.</p>
<p><strong>Structured, predictable output.</strong> Tools should return consistent schemas regardless of the internal code path taken. A tool that returns a dict in success cases and a string in failure cases forces the model to handle multiple output shapes — and it will handle them inconsistently. Always return a typed, structured result. Make failure states as information-dense as success states — the model needs failure details to decide what to do next.</p>
<p><strong>Idempotency where possible.</strong> Agents retry. If your tool has side effects — writes, sends, charges — idempotency prevents those side effects from compounding on retry. Include an idempotency key in any tool that performs a write operation.</p>
<p><strong>Hard usage limits.</strong> Tools that make external writes or have financial consequences should have rate limits enforced in the tool layer, not just in prompting. The model cannot be reliably instructed to self-limit. Enforce limits structurally.</p>
<h3 id="common-tool-anti-patterns">Common Tool Anti-Patterns</h3>
<pre tabindex="0"><code>Anti-pattern         What goes wrong
──────────────────────────────────────────────────────────────
God tool             One call does everything. No intermediate
                     checkpoint. Failure is unattributable.

Under-described      A tool named &#34;search&#34; with no description
schema               of what it searches, what format queries
                     take, or what the output looks like.
                     The model guesses. Sometimes correctly.

Swallowed errors     Returns {&#34;status&#34;: &#34;ok&#34;, &#34;result&#34;: null}
                     on failure. Model interprets &#34;ok&#34; as
                     success, receives null, produces nonsense.

Missing retrieval    The agent hallucinates because no tool
coverage             covers a data source it needs. Audit
                     coverage before you ship.
──────────────────────────────────────────────────────────────
</code></pre><hr>
<h2 id="multi-agent-coordination-when-one-agent-isnt-enough">Multi-Agent Coordination: When One Agent Isn&rsquo;t Enough</h2>
<p>Single-agent architectures hit two ceilings as task complexity grows: context window capacity and specialization. Multi-agent architectures address both by decomposing tasks across specialized agents that each operate with lean, relevant context.</p>
<p>This is not free. Coordination introduces failure modes that don&rsquo;t exist in single-agent systems.</p>
<h3 id="the-orchestrator-worker-pattern">The Orchestrator-Worker Pattern</h3>
<pre tabindex="0"><code>┌─────────────────────────────────────────────────┐
│                  Orchestrator                   │
│                                                 │
│  Decomposes goal → delegates sub-tasks →        │
│  synthesizes results → determines next steps    │
└──────────┬──────────────────────┬───────────────┘
           │                      │
           ▼                      ▼
┌──────────────────┐   ┌──────────────────────────┐
│   Worker A       │   │   Worker B               │
│                  │   │                          │
│ Domain-specific  │   │ Domain-specific           │
│ tools + context  │   │ tools + context           │
│                  │   │                          │
│ Executes sub-task│   │ Executes sub-task        │
│ independently    │   │ independently            │
└──────────┬───────┘   └──────────────┬───────────┘
           │                          │
           └────────────┬─────────────┘
                        ▼
              Results composed back
              by the orchestrator
</code></pre><p>The design decisions that determine whether this works:</p>
<p><strong>Sub-task interface design.</strong> The sub-task description must be self-contained — it cannot assume the worker has access to the orchestrator&rsquo;s broader context. This is the most commonly underestimated challenge. Poorly scoped sub-task descriptions produce workers that misinterpret their assignment and return irrelevant results.</p>
<p><strong>Result schema standardization.</strong> Worker agents must return results in a schema the orchestrator can reason about reliably. Define a standard result envelope: status, payload, confidence indicator if applicable, and a brief human-readable summary the orchestrator can use for planning.</p>
<p><strong>Failure propagation.</strong> When a worker fails, the orchestrator needs to know whether the sub-task failed completely, partially, or produced a low-confidence result. A binary success/failure signal is insufficient for a planning agent that may have recovery options.</p>
<h3 id="shared-state-and-write-ownership">Shared State and Write Ownership</h3>
<p>In multi-agent systems, coordination memory is a shared persistent store that all agents can read and write. The critical design decision is <strong>write ownership</strong>. In a well-designed multi-agent system, each piece of shared state has exactly one agent responsible for writing it. Concurrent writes from multiple agents without coordination produce race conditions that exhibit as intermittent, mysterious failures in production. Use optimistic locking or task-claim mechanisms to enforce write ownership at the state layer — not at the prompt layer.</p>
<h3 id="when-multi-agent-is-the-wrong-choice">When Multi-Agent Is the Wrong Choice</h3>
<p>Multi-agent architecture is frequently over-applied. It is the right choice when the task genuinely exceeds the planning horizon of a single model, when different sub-tasks require domain-specific context that shouldn&rsquo;t bleed across a monolithic window, or when parallelism is a hard latency requirement.</p>
<p>It is the wrong choice when coordination overhead exceeds task complexity, when a single agent with good context management would suffice, or when the team doesn&rsquo;t yet have robust observability across agent boundaries. Many systems that claim to need multi-agent coordination are actually single agents with context management problems. Fix the context management first.</p>
<hr>
<h2 id="observability-tracing-the-loop-not-just-the-edges">Observability: Tracing the Loop, Not Just the Edges</h2>
<p>Standard application monitoring tracks requests and responses. Agent observability has to track the loop — every iteration, every tool call, every reasoning step, and the token budget at each point. A single user request can produce dozens of model calls and tool executions. You need to correlate all of them into a coherent session trace.</p>
<h3 id="what-to-emit-at-every-loop-iteration">What to Emit at Every Loop Iteration</h3>
<p>This is the minimum viable trace event. Emit one per iteration, structured, before the next iteration begins:</p>
<pre tabindex="0"><code>Iteration trace event
─────────────────────────────────────────────
session_id            Ties all iterations together
iteration_n           Which loop cycle this is
context_tokens        Tokens in context at start
thought               Model&#39;s reasoning output
tool_name             Tool selected (or &#34;none&#34;)
tool_params           Parameters as constructed
tool_latency_ms       Wall time for tool execution
tool_result_size      Tokens in tool response
context_tokens_after  Tokens in context after update
error                 null if clean; typed if not
timestamp_ms          Unix ms at iteration start
</code></pre><p>Context tokens before and after is the most important pair. The delta tells you how fast the window is filling. If <code>context_tokens_after</code> is growing faster than <code>context_tokens</code> is shrinking from summarization, you will hit degradation before the task completes.</p>
<h3 id="the-session-trace-structure">The Session Trace Structure</h3>
<p>A complete session trace is a tree, not a flat log:</p>
<pre tabindex="0"><code>session: s_abc123
│
├── iteration: 1
│   ├── model_call: thought generation      (320ms, 1,840 tokens in)
│   ├── tool_call:  search_knowledge_base   (210ms, result: 420 tokens)
│   └── context snapshot: 2,260 tokens
│
├── iteration: 2
│   ├── model_call: thought generation      (290ms, 2,260 tokens in)
│   ├── tool_call:  call_crm_api            (850ms, result: 180 tokens)
│   └── context snapshot: 2,440 tokens
│
├── iteration: 3
│   ├── model_call: thought generation      (340ms, 2,440 tokens in)
│   ├── tool_call:  summarize_context       (-1,100 tokens evicted)
│   └── context snapshot: 1,520 tokens     ← summarization fired
│
└── iteration: 4
    ├── model_call: thought generation      (305ms, 1,520 tokens in)
    ├── tool_call:  send_email              (120ms, result: 40 tokens)
    └── terminal: goal_achieved
</code></pre><p>The context snapshot after iteration 3 shows summarization working correctly — the window dropped from 2,440 to 1,520 tokens. Without this tree structure, you can&rsquo;t see that event or attribute subsequent behavior to it.</p>
<h3 id="latency-attribution">Latency Attribution</h3>
<p>Total session latency breaks down into four buckets. You cannot optimize what you don&rsquo;t attribute:</p>
<pre tabindex="0"><code>Session latency breakdown
─────────────────────────────────────────────────────
                                           % of total
Model inference latency     ████████████     48%
Tool execution latency      ████████████████ 38%
  └─ call_crm_api  ████████ 22%
  └─ search_kb     ████     10%
  └─ send_email    ██        6%
Context assembly latency    ████              9%
Orchestration overhead      █                5%
─────────────────────────────────────────────────────
Total session wall time: 4,820ms
</code></pre><p>In most production agents, tool latency dominates — not model inference. You find this only with per-tool timing. Without attribution, optimization effort lands on the wrong layer.</p>
<h3 id="error-classification">Error Classification</h3>
<p>Mixing error types in a single metric makes debugging impossible. Classify every error at emission time:</p>
<pre tabindex="0"><code>Error taxonomy
──────────────────────────────────────────────────────────────
Class           Type              Recovery action
──────────────────────────────────────────────────────────────
Model errors
  malformed_tool_call   Return structured error to model,
                        allow one retry with correction hint
  goal_drift            Reinject original goal, flag for
                        human review if recurs
  reasoning_loop        Detect via repeated tool calls,
                        terminate and surface to operator

Tool errors
  transient_failure     Exponential backoff, max 3 retries
  permanent_failure     Surface to model as context update,
                        trigger replanning
  parameter_invalid     Return typed error with correction
                        schema, allow model to revise call
  timeout               Log latency, treat as transient,
                        apply retry policy

Orchestration errors
  context_overflow      Emergency summarization before
                        next model call
  termination_failure   Hard stop, checkpoint state, alert
  state_corruption      Halt immediately, do not recover,
                        escalate
──────────────────────────────────────────────────────────────
</code></pre><h3 id="alert-rules">Alert Rules</h3>
<pre tabindex="0"><code>Alert rules
──────────────────────────────────────────────────────────────
Signal                  Threshold       Action
──────────────────────────────────────────────────────────────
Context tokens          &gt; 60% limit     Trigger summarization
Context tokens          &gt; 80% limit     Force summarization,
                                        suspend iteration
Tool error rate         &gt; 2/session     Log, notify on-call
                                        if task is high-stakes
Repeated tool call      Same tool ≥ 3   Detect retry spiral,
                        consecutive     terminate session
Session duration        &gt; P99 baseline  Flag for review
Termination condition   Not reached     Hard stop at max
not triggered           by max_iter     iteration limit
State corruption        Any occurrence  Halt immediately
──────────────────────────────────────────────────────────────
</code></pre><p>The repeated tool call rule catches the most destructive failure mode: a retry spiral where the agent calls the same failing tool indefinitely. This burns tokens, time, and potentially triggers external side effects on every call.</p>
<hr>
<h2 id="production-guardrails-and-recovery">Production Guardrails and Recovery</h2>
<h3 id="guardrails-constraining-the-action-space">Guardrails: Constraining the Action Space</h3>
<p>Guardrails are structural constraints on what the agent can do. They are enforced in the orchestration and tool layers — not in prompting — and cannot be overridden by model output.</p>
<p><strong>Input guardrails</strong> validate the goal and context before the loop begins. Malformed goals, goals that reference unavailable tools, or goals that exceed defined scope should be rejected at intake — not discovered three iterations into an expensive loop.</p>
<p><strong>Tool call validation</strong> checks every tool call the model generates before execution. Validate parameter types, check values against allowed ranges, and verify the requested tool is in scope. Reject malformed calls and return a structured error to the model. The model can often recover from a rejected call if the error message is informative.</p>
<p><strong>Output guardrails</strong> screen the agent&rsquo;s final output before it&rsquo;s returned or acted on. For agents that produce content consumed by users, check for policy violations or hallucinated citations. For agents that take real-world actions, validate that the proposed action is within defined operational bounds before execution.</p>
<p><strong>Rate and scope limits</strong> enforce hard ceilings on the real-world impact any single session can have: maximum API calls per tool per session, maximum financial transactions per hour, maximum records modified per run. These live at the infrastructure layer — not in the prompt.</p>
<h3 id="human-in-the-loop-checkpoints">Human-in-the-Loop Checkpoints</h3>
<p>Not every action should be auto-executed. High-stakes, irreversible, or high-uncertainty actions should route through a human approval checkpoint before execution. The classification logic — which actions are always auto-approved, always human-approved, and conditionally approved — must be enumerated explicitly in a policy configuration that the orchestration layer enforces. Don&rsquo;t rely on the model to classify this.</p>
<h3 id="failure-recovery">Failure Recovery</h3>
<p><strong>Structured error handling at the tool layer</strong> means every tool returns a consistent failure schema with a failure type (transient, permanent, recoverable), a retry recommendation, and a human-readable explanation. The orchestration layer uses this to route failures correctly.</p>
<p><strong>Loop termination conditions</strong> must be explicitly defined. The loop ends when: the goal is achieved, a stopping condition fires (maximum iterations reached, token budget exhausted, error threshold exceeded), or a terminal failure occurs. Without explicit termination logic, loops run until they hit a hard timeout — wasting resources and potentially taking partial, inconsistent actions along the way.</p>
<p><strong>State checkpointing</strong> persists the agent&rsquo;s current state at defined intervals so that a crash mid-task can be resumed rather than restarted from scratch. The checkpoint includes: current goal, completed steps, tool results obtained, and the current context summary.</p>
<p><strong>Audit trails</strong> record every action taken, every tool called, and every model decision made during a session. For agents that modify external state, the audit trail should include enough information to reverse each action — not just that an action was taken. Design the trail as if you&rsquo;ll need to reconstruct and undo a session in production under time pressure. You will.</p>
<hr>
<h2 id="the-guiding-principles">The Guiding Principles</h2>
<p>Design memory like a thoughtful schema: persist what must survive, reconstruct what can be rebuilt cheaply, never store what shouldn&rsquo;t be retained.</p>
<p>Design tools like clean API contracts: explicit, single-purpose, hard to misuse.</p>
<p>Design orchestration around failure, not the happy path: termination conditions, error classification, retry bounds, and checkpointing are not edge case concerns — they are the architecture.</p>
<p>Design coordination for correctness before performance: enforce write ownership structurally, enumerate dependencies explicitly, and introduce parallelism only when sequential execution has proven insufficient.</p>
<p>Instrument the loop, not just the edges: the inputs and final outputs of a session tell you almost nothing about why it failed. The per-iteration trace — token trajectory, tool selection, latency attribution, error classification — tells you everything.</p>
<p>The model gets the hype. Memory, tools, orchestration, observability, and operations determine whether your agent becomes a reliable system or a cautionary tale.</p>
]]></content:encoded></item><item><title>Hosting Local LLMs on Kubernetes: A Complete Enterprise Architecture Guide</title><link>https://blog.yottadynamics.com/posts/hosting-local-llms-on-kubernetes/</link><pubDate>Mon, 06 Apr 2026 09:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/hosting-local-llms-on-kubernetes/</guid><description>A deep-dive into every layer of a production-grade, fully open-source stack for self-hosting large language models — from the API gateway to the GPU compute plane.</description><category>ai-infrastructure</category><category>kubernetes</category><category>platform-engineering</category><category>operations</category><category>architecture</category><enclosure url="https://blog.yottadynamics.com/images/posts/hosting-local-llms-on-kubernetes.png" type="image/svg+xml"/><content:encoded><![CDATA[<p><em>A deep-dive into every layer of a production-grade, fully open-source stack for self-hosting large language models — from the API gateway to the GPU compute plane.</em></p>
<hr>
<h2 id="why-self-host">Why self-host?</h2>
<p>Cloud-hosted LLM APIs are convenient, but they come with trade-offs that matter at enterprise scale: data leaves your network on every inference call, costs scale linearly with volume (and models are getting longer), and you have no control over model versioning, rate limits, or uptime SLAs. Self-hosting on Kubernetes gives you full control over the stack — at the cost of having to build and operate that stack yourself. (<a href="/posts/get-started-hosting-your-own-llms-using-ollama/">Ollama</a> is the much simpler single-machine alternative if you&rsquo;re not at production scale yet.)</p>
<p>This guide covers every layer of a production LLM serving platform using exclusively open-source tools. We&rsquo;ll go from a raw HTTP request all the way down to GPU silicon, explaining why each component exists and how they fit together.</p>
<hr>
<h2 id="architecture-overview">Architecture overview</h2>
<p>The full stack has seven layers, each solving a distinct set of problems:</p>
<pre tabindex="0"><code>┌─────────────────────────────────────────────────────────────────┐
│                         CLIENTS                                 │
│          Web apps · Mobile · CLI agents · Internal APIs         │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTPS
┌───────────────────────────▼─────────────────────────────────────┐
│                   API GATEWAY LAYER                             │
│   cert-manager (TLS) · Keycloak (OIDC) · Rate limiting         │
│   WAF · LLM Guard · Envoy / Kong / Traefik + Gateway API       │
└───────────────────────────┬─────────────────────────────────────┘
                            │ cache hit? return early
┌───────────────────────────▼─────────────────────────────────────┐
│                    LLM CACHE LAYER                              │
│        Exact-match (Redis) · Semantic (Qdrant) · KV prefix      │
└───────────────────────────┬─────────────────────────────────────┘
                            │ cache miss → forward to inference
┌───────────────────────────▼─────────────────────────────────────┐
│                  INFERENCE LAYER (vLLM)                         │
│   Router · Tensor parallelism · Pipeline parallelism            │
│   Continuous batching · Paged attention · Speculative decoding  │
└───────────────────────────┬─────────────────────────────────────┘
                            │ kernel calls
┌───────────────────────────▼─────────────────────────────────────┐
│                  GPU COMPUTE LAYER                              │
│     NVIDIA device plugin · MIG · NVLink/RoCE · KEDA autoscale  │
└───────────────────────────┬─────────────────────────────────────┘
                            │ model weights loaded from
┌───────────────────────────▼─────────────────────────────────────┐
│               MODEL STORAGE &amp; REGISTRY                          │
│          MinIO (S3) · MLflow · Rook-Ceph · Init containers      │
└─────────────────────────────────────────────────────────────────┘

    Cross-cutting concerns (all layers)
    ├── Observability: Prometheus · Grafana · Jaeger · Loki
    └── Control plane: ArgoCD · Vault · Istio / Cilium · OPA Gatekeeper
</code></pre><p>Let&rsquo;s walk through each layer in detail.</p>
<hr>
<h2 id="layer-1-the-api-gateway">Layer 1: The API gateway</h2>
<p>The gateway is the single entry point for all LLM traffic. It does a lot of work before a single token gets generated.</p>
<h3 id="why-kubernetes-gateway-api-not-legacy-ingress">Why Kubernetes Gateway API (not legacy Ingress)?</h3>
<p>The older Kubernetes Ingress API conflates infrastructure and application concerns. The <strong>Kubernetes Gateway API</strong> (GA and mature in 2026) separates them cleanly:</p>
<ul>
<li><code>GatewayClass</code> — infrastructure team owns the controller (Envoy Gateway, Kong, Traefik, etc.).</li>
<li><code>Gateway</code> — defines listeners, TLS, and ports.</li>
<li><code>HTTPRoute</code> — application/ML teams define routing per model endpoint.</li>
</ul>
<p>This separation is essential in enterprises where platform and ML teams are distinct.</p>
<h3 id="tls-termination">TLS termination</h3>
<p>Use <code>cert-manager</code> for automated certificates (Let&rsquo;s Encrypt for public endpoints or internal CA for private clusters). The gateway terminates TLS; internal east-west traffic uses mTLS via the service mesh.</p>
<h3 id="authentication-and-authorization">Authentication and authorization</h3>
<p>Use <strong>OIDC/OAuth2</strong> with <strong>Keycloak</strong> (or equivalent). Map scopes to models for cost governance — for example, <code>scope:large-model</code> unlocks higher-tier inference and can gate access to expensive 70B+ parameter models.</p>
<h3 id="rate-limiting">Rate limiting</h3>
<p>LLMs require dual limits:</p>
<ul>
<li>Requests per minute (protect against abuse).</li>
<li>Tokens per hour/day (prevent cost overruns).</li>
</ul>
<p>Implement via Envoy rate-limit service (backed by Redis) or native plugins in Kong/Traefik. Charge token usage asynchronously after generation.</p>
<h3 id="waf-ddos-protection-and-prompt-guarding">WAF, DDoS protection, and prompt guarding</h3>
<p>Apply OWASP Core Rule Set (via Envoy Lua/WASM or ModSecurity) and a lightweight <strong>LLM Guard</strong> sidecar for prompt injection, PII scrubbing, and toxicity scoring.</p>
<h3 id="audit-logging-and-distributed-tracing">Audit logging and distributed tracing</h3>
<p>Emit OpenTelemetry spans for every request (user identity, model, token counts, cache status, latency breakdown). Ship to Jaeger + Prometheus + Loki.</p>
<h3 id="gateway-implementation-choices">Gateway implementation choices</h3>
<table>
  <thead>
      <tr>
          <th>Option</th>
          <th>Best for</th>
          <th>Notes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Envoy Gateway</strong></td>
          <td>Performance &amp; control</td>
          <td>Strong Gateway API conformance; excellent for custom AI logic</td>
      </tr>
      <tr>
          <td><strong>Kong Gateway OSS</strong></td>
          <td>Plugin ecosystem</td>
          <td>Rich out-of-box plugins for auth, rate limiting, AI features</td>
      </tr>
      <tr>
          <td><strong>Traefik</strong></td>
          <td>Simplicity &amp; GitOps</td>
          <td>Excellent Kubernetes-native experience</td>
      </tr>
  </tbody>
</table>
<p>All three support the Gateway API spec. Choose based on team expertise. Emerging options like Agentgateway add AI-specific routing (agent-to-agent traffic, tool call routing) that may matter for <a href="/posts/ai-agent-architecture-memory-tools-orchestration/">multi-agent workloads</a>.</p>
<hr>
<h2 id="layer-2-the-llm-cache">Layer 2: The LLM cache</h2>
<p>Cache hits eliminate GPU work entirely — this is the highest-leverage optimization in the stack.</p>
<h3 id="three-tiers-of-caching">Three tiers of caching</h3>
<p><strong>1. Exact-match cache (Redis/Valkey)</strong></p>
<p>SHA256 of (model + messages + parameters). Great for FAQs, repeatable batch jobs, and any deterministic prompt patterns. Typical hit rate: 5–20%.</p>
<p><strong>2. Semantic cache (GPTCache + Qdrant, or Redis with vector search)</strong></p>
<p>Embedding-based cosine similarity lookup. Tune the threshold carefully — 0.92–0.95 for factual work, disabled for creative tasks. Typical hit rate: 15–40%. The tradeoff is a small added latency for the embedding lookup and risk of incorrect hits if your threshold is too loose.</p>
<p><strong>3. vLLM prefix KV cache (GPU-resident)</strong></p>
<p>vLLM hashes KV blocks for shared system prompts or <a href="/posts/deep-dive-retrieval-augmented-generation-rag/">RAG</a> contexts and reuses them across requests. This is the most powerful tier: for chatbot or RAG workloads where most requests share the same system prompt, prefix cache hit rates of 60–90% are common, dramatically cutting time-to-first-token (TTFT).</p>
<p>Combine all three tiers. The exact-match check is microseconds; the semantic check adds a few milliseconds; the prefix cache operates within vLLM transparently.</p>
<hr>
<h2 id="layer-3-vllm-inference">Layer 3: vLLM inference</h2>
<p><strong>vLLM</strong> remains the highest-throughput open-source inference engine for LLMs. Its core innovations are worth understanding because they directly shape how you size and operate it.</p>
<h3 id="paged-attention">Paged attention</h3>
<p>Traditional inference pre-allocates a contiguous KV cache block per sequence. vLLM uses paged attention — non-contiguous physical blocks allocated on demand — which eliminates memory fragmentation and enables much higher concurrent sequence counts on the same GPU memory.</p>
<h3 id="continuous-batching">Continuous batching</h3>
<p>Rather than processing a fixed batch then starting a new one, vLLM uses continuous batching (also called iteration-level scheduling): new requests join the batch mid-iteration as soon as a slot frees. This eliminates GPU idle time between requests and is a primary driver of throughput improvement over naive serving.</p>
<h3 id="prefix-caching">Prefix caching</h3>
<p>vLLM computes and caches KV blocks for common prefixes (system prompts, RAG contexts). Subsequent requests that share the prefix skip the prefill computation for those tokens entirely. At scale, this is often the difference between a cluster that fits in budget and one that doesn&rsquo;t.</p>
<h3 id="parallelism-strategies">Parallelism strategies</h3>
<p>For models exceeding single-GPU VRAM:</p>
<ul>
<li><strong>Tensor parallelism</strong>: Splits weight matrices across GPUs within a node. Uses NVLink/NVSwitch for fast all-reduce. Scales to 8 GPUs per node efficiently.</li>
<li><strong>Pipeline parallelism</strong>: Splits model layers across nodes. Uses RoCE or InfiniBand for inter-node communication. Introduces pipeline bubbles but enables serving models that won&rsquo;t fit on a single node.</li>
</ul>
<p>Combine both for the largest models.</p>
<h3 id="quantization">Quantization</h3>
<p>Production recommendations in approximate priority order:</p>
<ul>
<li><strong>FP8</strong> (H100/H200): Near-lossless quality, ~2× memory reduction, supported natively in hardware.</li>
<li><strong>AWQ</strong>: Excellent quality/size tradeoff for A100 and older hardware.</li>
<li><strong>GPTQ</strong>: Widely supported, slightly lower quality than AWQ at equivalent bit-width.</li>
</ul>
<p>Leave 10–15% GPU memory headroom above your model&rsquo;s requirements for KV cache and kernel overhead.</p>
<h3 id="context-length-as-an-operational-lever">Context length as an operational lever</h3>
<p><code>max_model_len</code> — the maximum sequence length vLLM will accept — directly controls KV cache memory consumption per slot. Longer contexts consume proportionally more KV cache memory, which reduces the number of concurrent sequences the engine can hold and increases TTFT.</p>
<p>Set <code>max_model_len</code> deliberately rather than leaving it at the model&rsquo;s architectural maximum. For most production workloads, a limit of 8k–32k tokens is sufficient and meaningfully improves throughput. Monitor <code>vllm:gpu_cache_usage_perc</code> — if it runs consistently above 85%, reducing <code>max_model_len</code> is often the fastest way to recover headroom before adding hardware.</p>
<h3 id="speculative-decoding">Speculative decoding</h3>
<p>A draft model generates candidate tokens at low cost; the target model verifies them in parallel. Effective for latency-sensitive workloads where output is predictable (code, structured data). Adds complexity — evaluate whether the latency gain justifies the operational overhead.</p>
<h3 id="advanced-disaggregated-inference">Advanced: disaggregated inference</h3>
<p>For very large-scale deployments (many thousands of requests per day), consider disaggregated inference: separate prefill pods (compute-intensive, large batches) from decode pods (memory-bandwidth-intensive, streaming). The <strong>llm-d</strong> project implements this pattern and integrates with vLLM. It adds significant operational complexity but can substantially improve hardware utilization at scale.</p>
<hr>
<h2 id="layer-4-gpu-compute">Layer 4: GPU compute</h2>
<h3 id="nvidia-gpu-operator">NVIDIA GPU Operator</h3>
<p>Install via Helm. It automates driver installation, device plugin deployment, DCGM exporter setup, and container toolkit configuration. Without it, GPU node management becomes a manual nightmare across OS upgrades and Kubernetes versions.</p>
<h3 id="gpu-sharing">GPU sharing</h3>
<ul>
<li><strong>MIG (Multi-Instance GPU)</strong>: Available on A100/H100+. Creates hardware-isolated partitions with dedicated memory slices and compute engines. The right choice when you need strict isolation between workloads (multi-tenant or mixed-criticality).</li>
<li><strong>Time-slicing</strong>: Software-level sharing configured via GPU Operator. Lower overhead than MIG, no memory isolation. Suitable for dev/test environments or homogeneous workloads.</li>
</ul>
<h3 id="high-speed-interconnects">High-speed interconnects</h3>
<p>NVLink/NVSwitch for intra-node GPU communication (tensor parallelism collectives). RoCE v2 or InfiniBand for inter-node communication (pipeline parallelism and distributed training). For inference-only clusters, RoCE with RDMA is typically sufficient and significantly cheaper than InfiniBand.</p>
<h3 id="autoscaling-with-keda">Autoscaling with KEDA</h3>
<p>CPU/GPU utilization is a poor signal for LLM autoscaling — a GPU can be 80% utilized but handling requests efficiently, or 20% utilized but with a growing queue. Use <strong>KEDA</strong> with a Prometheus trigger on <code>vllm:num_requests_waiting</code>.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#f92672">apiVersion</span>: <span style="color:#ae81ff">keda.sh/v1alpha1</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">kind</span>: <span style="color:#ae81ff">ScaledObject</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">metadata</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">name</span>: <span style="color:#ae81ff">vllm-scaledobject</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">namespace</span>: <span style="color:#ae81ff">inference</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">spec</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">scaleTargetRef</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">name</span>: <span style="color:#ae81ff">vllm-deployment</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">minReplicaCount</span>: <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">maxReplicaCount</span>: <span style="color:#ae81ff">8</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">triggers</span>:
</span></span><span style="display:flex;"><span>  - <span style="color:#f92672">type</span>: <span style="color:#ae81ff">prometheus</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">metadata</span>:
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">serverAddress</span>: <span style="color:#ae81ff">http://prometheus.monitoring.svc:9090</span>
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">query</span>: <span style="color:#ae81ff">sum(vllm:num_requests_waiting{model=&#34;llama-3-70b&#34;})</span>
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">threshold</span>: <span style="color:#e6db74">&#34;8&#34;</span>
</span></span></code></pre></div><p>Target approximately 5–10 pending requests per replica as your threshold. Combine with Cluster Autoscaler or Karpenter for node-level scaling (GPU nodes take 3–5 minutes to join; plan for that latency in your scale-out strategy).</p>
<h3 id="readiness-probes-and-rolling-deploys">Readiness probes and rolling deploys</h3>
<p>vLLM takes several minutes to load weights before it can serve requests — 5–10 minutes for a 70B model depending on storage throughput. Without a correctly configured readiness probe, Kubernetes will route traffic to pods that are still loading and immediately return errors.</p>
<p>Configure the readiness probe against vLLM&rsquo;s <code>/health</code> endpoint with a sufficiently long <code>initialDelaySeconds</code> (or use <code>startupProbe</code> with a high <code>failureThreshold</code> to avoid timing fights). Set <code>terminationGracePeriodSeconds</code> long enough to drain in-flight streaming responses — 120–300 seconds is typical. During a rolling deploy, the old replica keeps serving until the new one passes its readiness check.</p>
<h3 id="graceful-scale-down">Graceful scale-down</h3>
<p>KEDA&rsquo;s default scale-down is aggressive. An in-progress streaming response can be 60+ seconds; a pod termination that fires mid-stream silently drops the connection. Set <code>scaleDown.stabilizationWindowSeconds</code> in the ScaledObject (300–600 seconds is reasonable) and configure a <code>preStop</code> lifecycle hook that waits for active connections to drain before the pod accepts SIGTERM. Pair this with <code>minReplicaCount: 1</code> to prevent complete scale-to-zero for latency-sensitive endpoints.</p>
<hr>
<h2 id="layer-5-model-storage-and-registry">Layer 5: Model storage and registry</h2>
<h3 id="getting-weights-into-minio">Getting weights into MinIO</h3>
<p>Most teams source weights from Hugging Face Hub. The practical pipeline: download once to a secure jump host or CI runner (<code>huggingface-cli download</code> or <code>hf_transfer</code> for speed), verify the SHA256 checksum against the Hub&rsquo;s published value, apply your quantization step if needed (AWQ/FP8 conversion offline before serving), then push to MinIO with versioned paths (<code>/models/llama-3-70b/awq-4bit/v1.2/</code>). Never pull directly from Hugging Face into production inference pods — it bypasses your checksum verification, creates an external dependency at pod startup, and is slow.</p>
<p>For air-gapped environments, mirror the weights to an internal registry during the ingestion pipeline and gate on checksum + license validation before the weights are considered promotion-eligible in MLflow.</p>
<h3 id="object-storage-minio">Object storage (MinIO)</h3>
<p>Store model weights in MinIO (S3-compatible). Version with prefixed paths (<code>/models/llama-3-70b/v2.1/</code>). Use init containers to pull weights into a shared volume before the inference pod starts, or mount directly via a CSI driver.</p>
<h3 id="model-registry-mlflow">Model registry (MLflow)</h3>
<p>Track model lineage, quantization config, evaluation results, and deployment history. Enables A/B testing via weighted routing in the gateway. Integrate with your CI/CD pipeline so model promotions are gated on evaluation thresholds.</p>
<h3 id="persistent-storage-rook-ceph">Persistent storage (Rook-Ceph)</h3>
<p>For RWX access patterns (multiple inference pods reading weights simultaneously), Rook-Ceph provides a self-managed distributed filesystem. Alternatively, MinIO with parallel downloads from multiple replicas works well if you cache locally on the node.</p>
<p>Always verify checksums after weight downloads — a corrupted weight file produces subtle, hard-to-diagnose inference errors.</p>
<hr>
<h2 id="layer-6-observability">Layer 6: Observability</h2>
<p>LLMs have different failure modes than typical services. Your dashboards need to reflect that.</p>
<h3 id="key-vllm-metrics">Key vLLM metrics</h3>
<table>
  <thead>
      <tr>
          <th>Metric</th>
          <th>What it tells you</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>vllm:num_requests_waiting</code></td>
          <td>Queue depth — primary autoscaling signal</td>
      </tr>
      <tr>
          <td><code>vllm:gpu_cache_usage_perc</code></td>
          <td>KV cache pressure — if consistently &gt;85%, add replicas or reduce context length</td>
      </tr>
      <tr>
          <td><code>vllm:prefix_cache_hit_rate</code></td>
          <td>Prefix cache effectiveness</td>
      </tr>
      <tr>
          <td><code>vllm:e2e_request_latency</code></td>
          <td>End-to-end latency histogram</td>
      </tr>
      <tr>
          <td><code>vllm:time_to_first_token</code></td>
          <td>TTFT — user-perceived responsiveness</td>
      </tr>
      <tr>
          <td><code>vllm:time_per_output_token</code></td>
          <td>TPOT — streaming speed</td>
      </tr>
  </tbody>
</table>
<h3 id="gpu-metrics-dcgm-exporter">GPU metrics (DCGM exporter)</h3>
<p>Monitor GPU utilization, memory bandwidth, NVLink throughput, and GPU temperature. Throttling events (SM clock drops) indicate thermal or power issues that degrade throughput without obvious errors.</p>
<h3 id="tracing-and-logging">Tracing and logging</h3>
<p>Propagate trace context from the gateway through to vLLM. Log request_id, user_id, model, token counts (prompt + completion), cache tier hit (exact/semantic/prefix), TTFT, TPOT, and any guardrail flags. This data is essential for cost attribution and debugging latency regressions.</p>
<hr>
<h2 id="layer-7-the-platform-control-plane">Layer 7: The platform control plane</h2>
<h3 id="gitops-argocd--flux">GitOps (ArgoCD / Flux)</h3>
<p>Every cluster resource — deployments, ScaledObjects, policies, secrets references — lives in Git. ArgoCD syncs it. No manual kubectl apply in production. This makes rollbacks, audits, and multi-cluster management tractable.</p>
<h3 id="secrets-management-hashicorp-vault">Secrets management (HashiCorp Vault)</h3>
<p>Dynamic secrets for database credentials, API keys, and model registry tokens. Use the Vault Agent sidecar or External Secrets Operator to inject secrets as environment variables or files. Avoid Kubernetes Secrets for anything sensitive — they&rsquo;re base64-encoded in etcd, not encrypted by default.</p>
<h3 id="service-mesh-istio--cilium">Service mesh (Istio / Cilium)</h3>
<p>mTLS for all east-west traffic. Zero-trust: pods cannot communicate unless explicitly permitted. Cilium (eBPF-based) has lower overhead than Istio&rsquo;s sidecar model and is the better choice for latency-sensitive inference traffic. Use Istio if you need its traffic management features (circuit breaking, retry policies, mirroring).</p>
<h3 id="policy-opa-gatekeeper">Policy (OPA Gatekeeper)</h3>
<p>Admission control policies that enforce:</p>
<ul>
<li>All GPU workloads must have resource limits set.</li>
<li>No <code>:latest</code> image tags in production namespaces.</li>
<li>All pods must carry cost-attribution labels (<code>team</code>, <code>model</code>, <code>environment</code>).</li>
<li>GPU nodes must have the appropriate taint/toleration pair.</li>
</ul>
<h3 id="network-policies">Network policies</h3>
<p>Restrict pod communication explicitly. Inference pods should only accept traffic from the gateway and observability scrape jobs. They should only egress to the model registry and observability collectors. Default-deny egress on inference namespaces prevents data exfiltration.</p>
<hr>
<h2 id="full-request-lifecycle">Full request lifecycle</h2>
<p>Putting it together, a single inference request flows through:</p>
<ol>
<li><strong>TLS termination</strong> at the gateway — client presents token.</li>
<li><strong>OIDC validation</strong> — Keycloak confirms identity, scope checked against requested model.</li>
<li><strong>Rate limit check</strong> — token budget and request rate verified in Redis.</li>
<li><strong>Prompt guard</strong> — LLM Guard sidecar scans for injection and PII.</li>
<li><strong>Exact-match cache check</strong> — SHA256 lookup in Redis.</li>
<li><strong>Semantic cache check</strong> — embedding lookup in Qdrant (on cache miss).</li>
<li><strong>vLLM routing</strong> — request forwarded to least-loaded replica.</li>
<li><strong>Prefix cache check</strong> — vLLM checks KV block hashes for shared prefix.</li>
<li><strong>Prefill</strong> — prompt tokens processed, KV cache populated.</li>
<li><strong>Decode</strong> — tokens generated and streamed back via SSE.</li>
<li><strong>Token accounting</strong> — usage logged asynchronously for cost attribution.</li>
<li><strong>Trace closed</strong> — span exported to Jaeger with full latency breakdown.</li>
</ol>
<p>Latency and cost are controlled at steps 5, 6, and 8. Observability at every step.</p>
<hr>
<h2 id="when-to-evolve-beyond-pure-vllm-deployments">When to evolve beyond pure vLLM deployments</h2>
<p>The stack described here is ideal for focused, high-performance serving of a small number of models. For larger-scale scenarios, consider layering additional tooling:</p>
<p><strong>KServe</strong> (with vLLM runtime or LLMInferenceService) adds a standardized control plane for multi-model governance, canary rollouts, and heterogeneous workloads (LLMs + embeddings + vision models). It keeps vLLM as the inference engine while providing higher-level abstractions for model lifecycle management.</p>
<p><strong>llm-d</strong> adds advanced distributed routing and disaggregated prefill/decode separation on top of vLLM. Worth evaluating when you have dedicated hardware for prefill compute and want to maximize utilization of decode capacity separately.</p>
<p>These are additive layers — they don&rsquo;t replace vLLM, they orchestrate it.</p>
<hr>
<h2 id="gaps-worth-filling-for-your-environment">Gaps worth filling for your environment</h2>
<p>This guide covers the core serving platform. Depending on your context, you&rsquo;ll also need to address:</p>
<ul>
<li><strong>Multi-tenancy</strong>: Dedicated namespaces per team (strong isolation, higher overhead) vs. shared inference pool with metering at the gateway (better utilization, more complex chargeback).</li>
<li><strong>Fine-tuning</strong>: Separate GPU node pools with tools like Axolotl or LitGPT. Never share fine-tuning and inference workloads on the same nodes — memory pressure from training jobs will degrade inference latency unpredictably.</li>
<li><strong>Multi-region / HA</strong>: Global load balancing across clusters + MinIO cross-region replication for weight distribution.</li>
<li><strong>Cost attribution</strong>: Token metering at the gateway + GPU-hour tracking via OpenCost or custom labels. Essential for chargeback and for identifying which teams/models are driving cost.</li>
<li><strong>A/B testing</strong>: Weighted routing in Gateway API HTTPRoute + Grafana dashboards comparing TTFT/TPOT/quality metrics between model versions.</li>
<li><strong>Security hardening</strong>: Image scanning in CI, runtime sandboxes (gVisor or Kata Containers for multi-tenant isolation), and prompt/response logging policies that avoid persisting sensitive data.</li>
</ul>
<hr>
<h2 id="summary-the-full-open-source-stack">Summary: the full open-source stack</h2>
<table>
  <thead>
      <tr>
          <th>Layer</th>
          <th>Function</th>
          <th>Tools</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>API Gateway</td>
          <td>Auth, rate limiting, routing</td>
          <td>Envoy / Kong / Traefik + Gateway API</td>
      </tr>
      <tr>
          <td>Identity</td>
          <td>Authn/Authz</td>
          <td>Keycloak</td>
      </tr>
      <tr>
          <td>Caching</td>
          <td>Reduce GPU compute</td>
          <td>Redis, Qdrant / GPTCache, vLLM prefix</td>
      </tr>
      <tr>
          <td>Inference</td>
          <td>Token generation</td>
          <td>vLLM (core) — optional KServe / llm-d</td>
      </tr>
      <tr>
          <td>GPU management</td>
          <td>Resource scheduling</td>
          <td>NVIDIA GPU Operator, MIG / time-slicing, KEDA</td>
      </tr>
      <tr>
          <td>Model storage</td>
          <td>Weight distribution</td>
          <td>MinIO, Rook-Ceph, MLflow</td>
      </tr>
      <tr>
          <td>Observability</td>
          <td>Metrics / traces / logs</td>
          <td>Prometheus, Grafana, Jaeger, Loki</td>
      </tr>
      <tr>
          <td>GitOps</td>
          <td>Config management</td>
          <td>ArgoCD / Flux</td>
      </tr>
      <tr>
          <td>Secrets</td>
          <td>Credential management</td>
          <td>HashiCorp Vault</td>
      </tr>
      <tr>
          <td>Network security</td>
          <td>Zero-trust mTLS</td>
          <td>Istio / Cilium</td>
      </tr>
      <tr>
          <td>Policy</td>
          <td>Admission control</td>
          <td>OPA Gatekeeper</td>
      </tr>
  </tbody>
</table>
<p>This stack delivers full data sovereignty, predictable costs, and high performance with no per-token pricing.</p>
<p>The operational investment is real. You need platform engineers who understand Kubernetes, GPU workloads, and distributed systems. But for teams with strict compliance requirements, high inference volume, or custom/fine-tuned models, self-hosting on Kubernetes is overwhelmingly worthwhile — and the tooling has matured to the point where the gap with managed services has narrowed significantly.</p>
]]></content:encoded></item><item><title>What Is an AI Agent? A Practical Guide for Builders</title><link>https://blog.yottadynamics.com/posts/what-is-an-ai-agent-a-practical-guide/</link><pubDate>Mon, 06 Apr 2026 09:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/what-is-an-ai-agent-a-practical-guide/</guid><description>The term &amp;lsquo;AI agent&amp;rsquo; gets applied to everything from a ChatGPT thread with a button to systems that autonomously manage deployments. That imprecision directly shapes the architectures you choose and the failure modes you inherit.</description><category>ai-agents</category><category>architecture</category><category>ai-infrastructure</category><enclosure url="https://blog.yottadynamics.com/images/posts/what-is-an-ai-agent-a-practical-guide.png" type="image/svg+xml"/><content:encoded><![CDATA[<p>Everyone&rsquo;s talking about AI agents. Most implementations are expensive chatbots with a marketing label.</p>
<p>The term gets applied to everything from a ChatGPT thread with a button to systems that autonomously manage deployments and customer operations. That imprecision isn&rsquo;t just annoying — it directly shapes the architectures you choose, the failure modes you inherit, and whether your &ldquo;agent&rdquo; actually ships value or becomes a production liability.</p>
<p>Let&rsquo;s fix that.</p>
<hr>
<h2 id="the-operational-definition">The Operational Definition</h2>
<p>An <strong>AI agent</strong> is an application that places a language model in a continuous loop: it perceives its environment through inputs and context, reasons about the next action, executes that action via tools, observes the result, and iterates — until the goal is achieved or a stopping condition fires.</p>
<p>Shorter: <strong>a language model in a loop, augmented with tools, to accomplish an objective.</strong></p>
<p>This think-act-observe cycle is what separates agents from static inference. The loop introduces probabilistic control flow, which is simultaneously the source of power and the root of every 2 a.m. incident. Understanding that trade-off in detail is the entire discipline.</p>
<pre tabindex="0"><code>┌───────────┐     ┌───────────┐     ┌───────────┐     ┌───────────┐
│  Perceive │────▶│  Reason   │────▶│    Act    │────▶│  Observe  │
│           │     │           │     │           │     │           │
│ Context + │     │ Plan next │     │ Call a    │     │ Update    │
│ inputs    │     │ action    │     │ tool      │     │ context   │
└───────────┘     └───────────┘     └───────────┘     └─────┬─────┘
      ▲                                                      │
      └──────────────────────────────────────────────────────┘
                     loops until goal is achieved
</code></pre><hr>
<h2 id="the-four-components">The Four Components</h2>
<p>Every agent is built from the same four parts. Three get the attention. One determines whether it survives production.</p>
<p><strong>The Model — the reasoning engine.</strong> The LLM analyzes context and decides what to do next. It never executes actions directly; it only plans them. Its output quality is entirely bounded by the quality of the context you feed it. Weak context produces confident stupidity.</p>
<p><strong>Tools — the interface to the real world.</strong> APIs, functions, databases, external services. Tools let the agent fetch live data, run computations, and take real-world actions. Without them, you have expensive autocomplete, not an agent.</p>
<p><strong>The Orchestration Layer — the nervous system.</strong> This owns the loop: planning, state tracking, <a href="/posts/how-memory-works-in-ai-agents/">memory retrieval</a>, tool dispatch, error recovery, and termination logic. Orchestration is what converts isolated model calls into coherent, goal-directed behavior. Most <a href="/posts/why-agents-fail-in-production/">production failures originate here</a>, not in the model.</p>
<p><strong>Runtime and Deployment Services — what separates prototypes from production.</strong> Monitoring, logging, security, human-in-the-loop approvals, and observability. Skipping this layer is the most common reason capable demos become unreliable systems.</p>
<pre tabindex="0"><code>┌─────────────────────────────────────────────┐
│               Agent session                 │
│                                             │
│   ┌─────────────┐      ┌─────────────────┐  │
│   │    Model    │◀────▶│  Orchestration  │  │
│   │  (reasons)  │      │  (owns the loop)│  │
│   └─────────────┘      └────────┬────────┘  │
│                                 │           │
│   ┌─────────────────────────────▼─────────┐ │
│   │                Tools                  │ │
│   │   APIs · functions · databases · RAG  │ │
│   └───────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
                     │
       ┌─────────────▼─────────────┐
       │    Runtime &amp; deployment   │
       │  observability · guardrails│
       │  logging · human-in-loop  │
       └───────────────────────────┘
</code></pre><hr>
<h2 id="chatbot-copilot-agent-different-architectures-not-a-capability-ladder">Chatbot, Copilot, Agent: Different Architectures, Not a Capability Ladder</h2>
<p>These aren&rsquo;t points on a spectrum — they&rsquo;re fundamentally different system designs with different failure modes and operational contracts.</p>
<pre tabindex="0"><code>┌─────────────┬──────────────┬──────────────┬──────────────────────────────┐
│             │   Chatbot    │   Copilot    │           Agent              │
├─────────────┼──────────────┼──────────────┼──────────────────────────────┤
│ State       │ Stateless    │Session-scoped│ Persistent                   │
│ Tools       │ None         │ Suggests     │ Executes                     │
│ Autonomy    │ Reactive     │ Assistive    │ Goal-directed                │
│ Fails via   │Hallucination │Bad suggestion│ Unexpected cascading action  │
│ Who catches │ You review   │ You act on   │ Nobody, unless you built     │
│ failures    │ output       │ output       │ the guardrails               │
└─────────────┴──────────────┴──────────────┴──────────────────────────────┘
</code></pre><p>The failure mode column is what matters. Agents introduce a new risk class: autonomous actions with real consequences — financial, legal, reputational. Design your system around its actual failure modes, not the ones you hope it avoids. Audit logs, guardrails, and rollback mechanisms aren&rsquo;t polish — they&rsquo;re load-bearing.</p>
<hr>
<h2 id="the-autonomy-spectrum">The Autonomy Spectrum</h2>
<p>Not every agent needs maximum autonomy. Matching autonomy level to the task is one of the most underrated architectural decisions.</p>
<pre tabindex="0"><code>Level 0 — Pure reasoning
  Model only. No tools, no state.
  Capable for analytical tasks where all needed context fits in the prompt.
  │
Level 1 — Connected
  Model + tools for data fetching and simple actions.
  Where most production agents actually live today.
  Most should stay here.
  │
Level 2 — Strategic
  Multi-step planning with cross-iteration context maintenance.
  The model reasons about sequences, not just immediate next steps.
  Context degradation becomes a real risk.
  │
Level 3 — Collaborative
  Multiple specialized agents coordinated by an orchestrator.
  Coordination overhead rises sharply.
  Communication failures become the new bottleneck.
  │
Level 4 — Self-evolving
  The system modifies its own tools, prompts, or behavior.
  Still largely research or heavily guarded production.
  Requires sandboxing and mandatory human oversight.
</code></pre><p>Most teams ship Level 1 while targeting Level 2. That gap is where reliability dies.</p>
<p><strong>The rule:</strong> start at the lowest level that solves the problem. Only move up when reliability is proven and the use case genuinely demands it. Higher levels add coordination complexity faster than they add value.</p>
<hr>
<h2 id="how-your-job-changes-as-a-builder">How Your Job Changes as a Builder</h2>
<p>Traditional software development is deterministic — you code every path explicitly. Agent development is closer to directing a capable but non-deterministic system: you define the goal, equip it with tools, craft the system prompt, and shape behavior through context at each iteration.</p>
<p>Control flow becomes probabilistic. The instinct is to compensate with endless conditionals and guardrails — but often that means fighting the architecture rather than leveraging it.</p>
<p>Your highest-leverage skill shifts from writing logic to <strong>context engineering</strong>: the deliberate assembly of what goes into the model&rsquo;s context window at each iteration. Every token costs latency, money, and signal quality. The model&rsquo;s reasoning quality is bounded entirely by the quality of this assembly.</p>
<p>Expect to spend more time on <a href="/posts/observability-for-ai-agents/">observability</a> and failure recovery than on the happy path. The happy path in an agent is a minority of real sessions.</p>
<hr>
<h2 id="the-questions-to-ask-before-you-build">The Questions to Ask Before You Build</h2>
<p>Skip &ldquo;Is this an agent?&rdquo; Ask: <strong>what level of autonomy does this task actually require?</strong></p>
<p>Before writing any orchestration code:</p>
<pre tabindex="0"><code>┌─────────────────────────────────────────────────────────────────────┐
│  Pre-build checklist                                                │
├─────────────────────────────────────────────────────────────────────┤
│  What specific decisions or actions will the system make           │
│  autonomously?                                                      │
│                                                                     │
│  What are the failure consequences, and what does recovery         │
│  look like?                                                         │
│                                                                     │
│  What triggers each loop iteration?                                │
│                                                                     │
│  What tools can it call, and what real-world impact can            │
│  those calls have?                                                  │
│                                                                     │
│  Are actions auto-executed or human-approved?                      │
│                                                                     │
│  How is failure detected, logged, and recovered?                   │
│  What audit trail exists?                                           │
│                                                                     │
│  What is the blast radius if a single session goes wrong?          │
└─────────────────────────────────────────────────────────────────────┘
</code></pre><p>Vague answers mean prototype. Production agents are observable, auditable, and recoverable by design — not as an afterthought.</p>
<hr>
<h2 id="whats-next">What&rsquo;s Next</h2>
<p>The definition, components, and autonomy model give you the mental framework. The harder questions are about the plumbing: how memory is designed, how tools are built to fail gracefully, how orchestration patterns differ and when to use each, how multi-agent systems coordinate without producing race conditions, and how observability is instrumented so you can debug a session after the fact.</p>
<p>That&rsquo;s what <a href="/posts/ai-agent-architecture-memory-tools-orchestration/">Part 2</a> covers.</p>
]]></content:encoded></item><item><title>Claude Code in the Terminal: The Deep-Dive</title><link>https://blog.yottadynamics.com/posts/claude-code-in-the-terminal/</link><pubDate>Wed, 01 Apr 2026 09:00:00 -0400</pubDate><guid>https://blog.yottadynamics.com/posts/claude-code-in-the-terminal/</guid><description>Most teams use Claude Code like a smart autocomplete. The teams getting real leverage treat it as an engineering platform — with CLAUDE.md, permissions, hooks, sub-agents, skills, and CI/CD integration designed in from the start.</description><category>developer-tools</category><category>ai-infrastructure</category><category>operations</category><category>platform-engineering</category><enclosure url="https://blog.yottadynamics.com/images/posts/claude-code-in-the-terminal.png" type="image/svg+xml"/><content:encoded><![CDATA[<p>Most teams use Claude Code like a smarter autocomplete. They open it, type a request, and close it. The teams getting real compounding leverage have done something different: they treat Claude Code as an engineering platform with a configuration layer, a <a href="/posts/how-memory-works-in-ai-agents/">memory system</a>, a permission model, a hook pipeline, a skills library, and a <a href="/posts/ai-agent-architecture-memory-tools-orchestration/">sub-agent runtime</a> — and they commit all of it to git.</p>
<p>This is the setup that makes that possible.</p>
<hr>
<h2 id="claudemd--the-project-brain">CLAUDE.md — the project brain</h2>
<p>CLAUDE.md is the most important file in your Claude Code setup. It is read automatically at session start, before any user message. It is your project&rsquo;s coding constitution, onboarding doc, and persistent context combined.</p>
<p>Resolution order (lowest to highest priority):</p>
<ol>
<li>Managed org policy (<code>/etc/claude-code/CLAUDE.md</code> on Linux, equivalent on macOS/Windows)</li>
<li>User global (<code>~/.claude/CLAUDE.md</code>)</li>
<li>Ancestor directories (walked upward from cwd)</li>
<li>Project root (<code>./CLAUDE.md</code> or <code>./.claude/CLAUDE.md</code>)</li>
<li>Subdirectory files (loaded on-demand when Claude enters that directory)</li>
<li>Personal overrides (<code>./CLAUDE.local.md</code> — <code>.gitignore</code> this)</li>
</ol>
<p>Use <code>@path/to/file.md</code> imports to break CLAUDE.md into modules. Place path-specific rules in <code>.claude/rules/</code> with YAML frontmatter. Prefix any message with <code>#</code> to write to CLAUDE.md instantly. Run <code>/init</code> on an existing project to generate a starter file from your repo automatically. Keep each file under 200 lines for best adherence.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span># Project: Payments API
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Tech Stack
</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Runtime: Node.js 20 + TypeScript 5.4 (strict mode)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Framework: Fastify 4 (NOT Express)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> ORM: Prisma 5 with PostgreSQL 16
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Testing: Vitest + Supertest
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Architecture Rules
</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> All business logic lives in <span style="color:#e6db74">`src/services/`</span> — controllers are thin
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> All monetary amounts stored in cents (integer), never floats
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Use <span style="color:#e6db74">`Result`</span> pattern (neverthrow) — never throw from service layer
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Every public service method must have a corresponding unit test
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Key Commands
</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`npm run dev`</span>         — start dev server (port 3000)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`npm run test`</span>        — run full test suite
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`npm run db:migrate`</span>  — apply pending Prisma migrations
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## PR Conventions
</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Branch: <span style="color:#e6db74">`feat/`</span>, <span style="color:#e6db74">`fix/`</span>, <span style="color:#e6db74">`chore/`</span>, <span style="color:#e6db74">`refactor/`</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Commits: Conventional Commits format
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Always run <span style="color:#e6db74">`npm run test &amp;&amp; npm run lint`</span> before opening a PR
</span></span></code></pre></div><hr>
<h2 id="memory-architecture">Memory architecture</h2>
<p>Three distinct layers operate at different scopes:</p>
<p><strong>Layer 1 — CLAUDE.md (explicit declarative memory).</strong> Human-written, version-controlled, always loaded. Ground truth for project knowledge.</p>
<p><strong>Layer 2 — Auto-memory (<code>~/.claude/projects/&lt;hash&gt;/memory/</code>).</strong> Claude writes structured files when it learns something worth persisting across sessions. <code>MEMORY.md</code> is the index, truncated at 200 lines. Disable with <code>CLAUDE_CODE_DISABLE_AUTO_MEMORY=1</code> for full manual control.</p>
<p><strong>Layer 3 — In-session context window.</strong> Lost on <code>/clear</code> or exit. Use <code>/compact</code> to summarize and compress before it fills. Use <code>/rewind</code> to return to any earlier checkpoint in the session.</p>
<p>The <code>/memory</code> command opens both CLAUDE.md and the auto-memory folder, shows which files are currently loaded, and lets you toggle auto-memory on or off.</p>
<hr>
<h2 id="context-management-and-compaction">Context management and compaction</h2>
<p>The context window fills up. How you handle that determines whether long sessions degrade or stay coherent.</p>
<p><strong><code>/compact</code></strong> summarizes the current conversation into a compressed handoff and continues from there. It preserves the goal and key decisions while discarding low-signal exchange history. Use it proactively — before the window is full, not after Claude starts losing track.</p>
<p><strong><code>/clear</code></strong> wipes the context entirely. Use it when starting a genuinely new task rather than letting unrelated history pollute the next one.</p>
<p><strong><code>/context</code></strong> shows current token usage — input tokens consumed, percentage of window used, and estimated remaining capacity. Check it before starting a long agentic run so you know whether to compact first.</p>
<p><strong><code>/rewind</code></strong> lets you time-travel to any previous checkpoint in the session. Every tool call creates an implicit checkpoint. If Claude takes a wrong turn, rewind to before it happened rather than trying to undo the effects manually.</p>
<p>For long-running projects, seed auto-memory with a handoff file: a structured <code>memory/MEMORY.md</code> containing a progress log, key decisions made, and a &ldquo;Next Session: Start Here&rdquo; section that gives the next session immediate orientation.</p>
<hr>
<h2 id="permission-system">Permission system</h2>
<p>Permissions control which tools Claude can invoke and when it needs to ask. Defined in <code>.claude/settings.json</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;permissions&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;allow&#34;</span>: [
</span></span><span style="display:flex;"><span>      <span style="color:#e6db74">&#34;Read&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#e6db74">&#34;Glob&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#e6db74">&#34;Grep&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#e6db74">&#34;Bash(npm run *)&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#e6db74">&#34;Bash(git diff*)&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#e6db74">&#34;Bash(git log*)&#34;</span>
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;deny&#34;</span>: [
</span></span><span style="display:flex;"><span>      <span style="color:#e6db74">&#34;Bash(rm *)&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#e6db74">&#34;Bash(git push*)&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#e6db74">&#34;WebFetch&#34;</span>
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p><code>allow</code> and <code>deny</code> both support glob patterns. <code>deny</code> takes precedence over <code>allow</code>. Tool names match Claude&rsquo;s internal tool names: <code>Read</code>, <code>Write</code>, <code>Edit</code>, <code>Bash</code>, <code>Glob</code>, <code>Grep</code>, <code>WebFetch</code>, <code>WebSearch</code>, <code>Agent</code>, etc.</p>
<p><strong>The five permission modes</strong> (set with <code>--permission-mode</code> or <code>/permissions</code>):</p>
<ul>
<li><strong><code>default</code></strong> — prompts for each tool call that isn&rsquo;t pre-approved. Safe for interactive sessions.</li>
<li><strong><code>acceptEdits</code></strong> — auto-approves file edits, prompts for Bash and network calls.</li>
<li><strong><code>plan</code></strong> — read-only mode. Claude can inspect files and reason but cannot write, run commands, or take action. Use this to review a plan before authorizing execution.</li>
<li><strong><code>auto</code></strong> — intelligent classifier approves low-risk actions automatically, prompts on high-risk ones. The right default for most interactive sessions once you trust the project setup.</li>
<li><strong><code>bypassPermissions</code></strong> — skips all prompts. Only safe inside isolated containers. Never use on a machine with credentials, live databases, or a production environment.</li>
</ul>
<p>For CI pipelines, combine <code>--permission-mode plan</code> with an explicit <code>--allowedTools</code> whitelist to give Claude exactly the access the job needs and nothing more.</p>
<hr>
<h2 id="plan-mode">Plan mode</h2>
<p>Plan mode (<code>--permission-mode plan</code> or <code>/plan</code>) is read-only. Claude can read files, search the codebase, reason about the problem, and produce a detailed implementation plan — but it cannot write files, run commands, or take any action.</p>
<p>Use it at the start of any non-trivial task:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>claude --permission-mode plan -p <span style="color:#e6db74">&#34;design the migration from REST to GraphQL for the orders service&#34;</span>
</span></span></code></pre></div><p>Claude will read the relevant code, reason through the approach, identify risks, and produce a step-by-step plan. Review it, adjust it, then re-run without <code>plan</code> to execute. This pattern eliminates the most common source of wasted agentic work: Claude executing a plausible-but-wrong approach for ten steps before you realize the direction was off.</p>
<p><code>/plan</code> inside an interactive session switches to plan mode for the current turn without changing the session&rsquo;s permission mode permanently.</p>
<hr>
<h2 id="checkpoints">Checkpoints</h2>
<p>Every tool call creates an implicit checkpoint — a snapshot of the conversation and file state at that point. Checkpoints are how <code>/rewind</code> works.</p>
<p>This matters for agentic runs. If Claude is making a sequence of file changes and takes a wrong turn at step 7, you do not need to manually undo each change. Use <code>/rewind</code> to select the checkpoint before the bad decision, adjust your instruction, and continue from there.</p>
<p>Checkpoints also matter when working with worktrees (<code>--worktree</code>). The worktree isolates Claude&rsquo;s changes from your working tree entirely — changes live in a separate git branch. You review the diff and merge what you want. Combined with checkpoints, this gives you a full undo tree for agentic work.</p>
<p>For long agentic runs in CI, <code>--max-turns</code> creates a hard ceiling on how many tool calls Claude can make before stopping. Use it as a cost and safety valve: if a run hits the limit, it exits cleanly and you can inspect the partial output rather than watching an uncontrolled loop run up a bill.</p>
<hr>
<h2 id="tasksmd--planning-agents-for-longer-runs">TASKS.md — planning agents for longer runs</h2>
<p>Background agents and headless <code>claude -p</code> runs share a fundamental problem: <a href="/posts/why-agents-fail-in-production/">the longer they run, the more likely they are to lose track</a> of where they are. Context fills, compaction summarizes away detail, and a multi-hour agent that forgets its own progress is worse than one that never started.</p>
<p><code>TASKS.md</code> solves this by giving the agent a durable, file-based working memory it can read and update throughout a long run. It is not a Claude Code built-in — it is a pattern you commit to your repo. The agent is instructed to treat it as ground truth for task state.</p>
<h3 id="why-a-file-not-session-memory">Why a file, not session memory</h3>
<p>Session memory lives in the context window. It compacts. It is lost on <code>/clear</code>. It does not survive if the process dies and you resume with <code>claude -c</code>.</p>
<p>A <code>TASKS.md</code> file:</p>
<ul>
<li>Survives compaction and context resets — Claude can read it back at any point</li>
<li>Gives you visibility into what the agent has done without parsing a long conversation</li>
<li>Can be committed to git, so the task state is part of the repo history</li>
<li>Works as a checkpoint list that <code>/rewind</code> can be anchored to</li>
</ul>
<p>For anything that takes more than twenty tool calls, a <code>TASKS.md</code> is the right scaffolding.</p>
<h3 id="structure">Structure</h3>
<p>Keep it simple and machine-readable. Status markers (<code>[ ]</code>, <code>[x]</code>, <code>[~]</code>) that Claude can update with a plain <code>Edit</code> call:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span># Task: Migrate payments service REST → GraphQL
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Status: IN PROGRESS
</span></span></span><span style="display:flex;"><span><span style="color:#75715e">## Started: 2026-04-10
</span></span></span><span style="display:flex;"><span><span style="color:#75715e">## Agent: claude-opus-4-6 | effort: high | max-turns: 150
</span></span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Phase 1 — Audit and schema design
</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [x]</span> Read all existing REST endpoints in <span style="color:#e6db74">`src/routes/payments/`</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [x]</span> Map request/response shapes to GraphQL types
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [x]</span> Draft <span style="color:#e6db74">`schema.graphql`</span> — reviewed and approved by human
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [ ]</span> Write resolvers for <span style="color:#e6db74">`getPayment`</span>, <span style="color:#e6db74">`listPayments`</span>, <span style="color:#e6db74">`createPayment`</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [ ]</span> Write resolvers for <span style="color:#e6db74">`refundPayment`</span>, <span style="color:#e6db74">`capturePayment`</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Phase 2 — Integration
</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [ ]</span> Wire resolvers to existing service layer (<span style="color:#e6db74">`src/services/payments.ts`</span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [ ]</span> Add DataLoader for N+1 query prevention on <span style="color:#e6db74">`listPayments`</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [ ]</span> Update integration tests from REST to GraphQL client
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Phase 3 — Cleanup
</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [ ]</span> Remove legacy REST route handlers
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [ ]</span> Update API documentation
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">- [ ]</span> Run full test suite, confirm green
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Decisions log
</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> 2026-04-10: Using <span style="color:#e6db74">`graphql-yoga`</span> (not Apollo) — lighter, Fastify-compatible
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> 2026-04-10: Keeping REST endpoints live behind feature flag during transition
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Blockers
</span></span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> None currently
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Next session: start here
</span></span></span><span style="display:flex;"><span>Resume at Phase 1 → &#34;Write resolvers for getPayment&#34;. Schema is final — do not modify.
</span></span></code></pre></div><h3 id="instructing-the-agent-to-use-it">Instructing the agent to use it</h3>
<p>In your CLAUDE.md or in the initial prompt:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span><span style="color:#75715e">## Long-running task protocol
</span></span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>When starting a task from TASKS.md:
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">1.</span> Read TASKS.md before doing anything else
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">2.</span> Find the first unchecked item and begin there
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">3.</span> After completing each item, mark it <span style="color:#e6db74">`[x]`</span> and save the file before moving on
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">4.</span> If you encounter a blocker, write it to the Blockers section and stop
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">5.</span> Before any compaction, update &#34;Next session: start here&#34; with exactly where to resume
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">6.</span> Never mark a phase complete until every item in it is checked
</span></span></code></pre></div><p>This means compaction or a context reset does not derail the run. The agent reads the file, finds its place, and continues. You get a full audit trail in git.</p>
<h3 id="pairing-tasksmd-with-background-agents">Pairing TASKS.md with background agents</h3>
<p>For tasks that run overnight or across working hours, combine TASKS.md with a background agent definition:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#75715e"># .claude/agents/migration-runner.yaml</span>
</span></span><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span><span style="color:#f92672">name</span>: <span style="color:#ae81ff">migration-runner</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">description</span>: <span style="color:#ae81ff">Execute multi-phase migrations from TASKS.md</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">model</span>: <span style="color:#ae81ff">claude-opus-4-6</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">effort</span>: <span style="color:#ae81ff">high</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">background</span>: <span style="color:#66d9ef">true</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">isolation</span>: <span style="color:#ae81ff">worktree</span>
</span></span><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">At session start, read TASKS.md and resume from the first unchecked item.</span>
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">Update task status after each completed item.</span>
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">Commit progress to the worktree branch at the end of each phase.</span>
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">Stop and write a Blockers entry if any phase fails — do not proceed to the next phase.</span>
</span></span></code></pre></div><p>Launch it:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>claude --agent migration-runner --max-turns <span style="color:#ae81ff">200</span> --max-budget-usd 10.00
</span></span></code></pre></div><p>Monitor progress with <code>/tasks</code> (shows live agent status) or by reading the <code>TASKS.md</code> file — which the agent is continuously updating as it works. When it finishes, review the worktree diff and merge what you want.</p>
<p>The pattern scales: one TASKS.md per long-running initiative, one background agent per task, <code>/tasks</code> as your control panel.</p>
<hr>
<h2 id="slash-commands-reference">Slash commands reference</h2>
<p>The commands used most often in practice:</p>
<p><strong>Context and memory</strong></p>
<table>
  <thead>
      <tr>
          <th>Command</th>
          <th>What it does</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>/clear</code></td>
          <td>Wipe conversation history and start fresh</td>
      </tr>
      <tr>
          <td><code>/compact</code></td>
          <td>Summarize and compress context, continue session</td>
      </tr>
      <tr>
          <td><code>/context</code></td>
          <td>Show token usage and remaining window capacity</td>
      </tr>
      <tr>
          <td><code>/rewind</code></td>
          <td>Return to any earlier session checkpoint</td>
      </tr>
      <tr>
          <td><code>/memory</code></td>
          <td>Open CLAUDE.md and auto-memory, toggle auto-memory</td>
      </tr>
  </tbody>
</table>
<p><strong>Configuration</strong></p>
<table>
  <thead>
      <tr>
          <th>Command</th>
          <th>What it does</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>/init</code></td>
          <td>Generate starter CLAUDE.md from current repo</td>
      </tr>
      <tr>
          <td><code>/model</code></td>
          <td>Switch model for the current session</td>
      </tr>
      <tr>
          <td><code>/permissions</code></td>
          <td>View and edit tool permissions interactively</td>
      </tr>
      <tr>
          <td><code>/config</code></td>
          <td>Open full settings editor</td>
      </tr>
      <tr>
          <td><code>/plan</code></td>
          <td>Switch current turn to plan (read-only) mode</td>
      </tr>
  </tbody>
</table>
<p><strong>Development workflow</strong></p>
<table>
  <thead>
      <tr>
          <th>Command</th>
          <th>What it does</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>/review</code></td>
          <td>Structured code review of recent changes</td>
      </tr>
      <tr>
          <td><code>/todos</code></td>
          <td>Show current task list</td>
      </tr>
      <tr>
          <td><code>/diff</code></td>
          <td>Interactive diff viewer for pending changes</td>
      </tr>
      <tr>
          <td><code>/simplify</code></td>
          <td>3-agent review pipeline for recently changed code</td>
      </tr>
      <tr>
          <td><code>/export</code></td>
          <td>Export conversation to a file</td>
      </tr>
  </tbody>
</table>
<p><strong>Tasks and agents</strong></p>
<table>
  <thead>
      <tr>
          <th>Command</th>
          <th>What it does</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>/tasks</code></td>
          <td>List and manage background tasks</td>
      </tr>
      <tr>
          <td><code>/batch</code></td>
          <td>Run parallel tasks in isolated worktrees</td>
      </tr>
      <tr>
          <td><code>/schedule</code></td>
          <td>Create a recurring scheduled task</td>
      </tr>
      <tr>
          <td><code>/effort</code></td>
          <td>Set reasoning effort level (Opus 4.6 only)</td>
      </tr>
  </tbody>
</table>
<p><strong>Diagnostics</strong></p>
<table>
  <thead>
      <tr>
          <th>Command</th>
          <th>What it does</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>/doctor</code></td>
          <td>Diagnose installation and configuration issues</td>
      </tr>
      <tr>
          <td><code>/insights</code></td>
          <td>Session analytics and usage summary</td>
      </tr>
      <tr>
          <td><code>/security-review</code></td>
          <td>Quick security scan of recent changes</td>
      </tr>
  </tbody>
</table>
<p>Custom commands go in <code>.claude/commands/</code> as Markdown files. They appear in the <code>/</code> menu automatically.</p>
<hr>
<h2 id="hooks--lifecycle-automation">Hooks — lifecycle automation</h2>
<p>Hooks attach shell commands, Haiku-based decisions, or sub-agents to events in Claude&rsquo;s execution lifecycle. They are the highest-leverage configuration most teams skip.</p>
<p>Available events:</p>
<ul>
<li><code>PreToolUse</code> / <code>PostToolUse</code> / <code>PostToolUseFailure</code></li>
<li><code>SessionStart</code> / <code>InstructionsLoaded</code> / <code>UserPromptSubmit</code></li>
<li><code>SubagentStart</code> / <code>SubagentStop</code></li>
<li><code>TaskCreated</code> / <code>TaskCompleted</code></li>
<li><code>PreCompact</code> / <code>PostCompact</code> / <code>PostSession</code></li>
</ul>
<p>Defined in <code>.claude/settings.json</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;hooks&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;PreToolUse&#34;</span>: [
</span></span><span style="display:flex;"><span>      {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;matcher&#34;</span>: { <span style="color:#f92672">&#34;tool&#34;</span>: <span style="color:#e6db74">&#34;Bash&#34;</span> },
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;echo &#39;[AUDIT] ${CLAUDE_TOOL_INPUT}&#39; &gt;&gt; ~/.claude/audit.log&#34;</span>
</span></span><span style="display:flex;"><span>      },
</span></span><span style="display:flex;"><span>      {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;matcher&#34;</span>: { <span style="color:#f92672">&#34;tool&#34;</span>: <span style="color:#e6db74">&#34;Bash&#34;</span> },
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;prompt&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;prompt&#34;</span>: <span style="color:#e6db74">&#34;Does this bash command look safe to run? Input: ${CLAUDE_TOOL_INPUT}. Reply ALLOW or BLOCK.&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;on_block&#34;</span>: <span style="color:#e6db74">&#34;abort&#34;</span>
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;PostToolUse&#34;</span>: [
</span></span><span style="display:flex;"><span>      {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;matcher&#34;</span>: { <span style="color:#f92672">&#34;tool&#34;</span>: <span style="color:#e6db74">&#34;Write&#34;</span> },
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;cd \&#34;$CLAUDE_PROJECT_DIR\&#34; &amp;&amp; npm run lint --silent || true&#34;</span>
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;PostSession&#34;</span>: [
</span></span><span style="display:flex;"><span>      {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;cd \&#34;$CLAUDE_PROJECT_DIR\&#34; &amp;&amp; npm test -- --reporter=dot 2&gt;&amp;1 | tail -5&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;async&#34;</span>: <span style="color:#66d9ef">true</span>
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Hook types:</p>
<ul>
<li><strong><code>command</code></strong> — run a shell command</li>
<li><strong><code>prompt</code></strong> — ask Haiku to make a decision (e.g. approve or block a tool call)</li>
<li><strong><code>agent</code></strong> — spawn a full sub-agent</li>
<li><strong><code>async: true</code></strong> — non-blocking; hook runs in background without holding up Claude</li>
</ul>
<p><code>PreToolUse</code> hooks fire even in <code>--dangerously-skip-permissions</code> mode. They are your last-resort guardrail regardless of permission settings.</p>
<hr>
<h2 id="skills--reusable-instruction-sets">Skills — reusable instruction sets</h2>
<p>Skills are reusable, parameterizable instruction sets that can be invoked like slash commands. They live in <code>.claude/skills/</code> (project-level) or <code>~/.claude/skills/</code> (global), each in their own folder with a <code>SKILL.md</code>.</p>
<pre tabindex="0"><code>.claude/skills/
├── pr-review/
│   └── SKILL.md
├── db-migration/
│   └── SKILL.md
└── api-contract/
    └── SKILL.md
</code></pre><p>A <code>SKILL.md</code> uses frontmatter to define the trigger, parameters, and description:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span>name: pr-review
</span></span><span style="display:flex;"><span>description: Full PR review — correctness, tests, security, and style
</span></span><span style="display:flex;"><span>trigger: /pr-review
</span></span><span style="display:flex;"><span>params:
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">-</span> name: focus
</span></span><span style="display:flex;"><span>    description: Optional area to focus on (security, performance, tests)
</span></span><span style="display:flex;"><span>    required: false
</span></span><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Review the current git diff thoroughly.
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>{{#if focus}}Focus especially on: {{focus}}.{{/if}}
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Check for:
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Logic errors and edge cases
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Missing or inadequate tests
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Security issues (injection, secrets exposure, insecure defaults)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Consistency with patterns in CLAUDE.md
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Anything that would fail in production but pass in a test environment
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Output a structured review with verdict (approve / request-changes), a summary, and a
</span></span><span style="display:flex;"><span>bulleted list of specific issues with file paths and line numbers.
</span></span></code></pre></div><p>Invoke with <code>/pr-review</code> or <code>/pr-review focus=security</code>. Skills compose with hooks — a <code>PostToolUse</code> hook can automatically trigger a skill after every <code>Write</code> call if you want continuous review on file changes.</p>
<hr>
<h2 id="sub-agents-and-parallel-execution">Sub-agents and parallel execution</h2>
<p>Sub-agents are Claude instances with their own context windows, models, tools, personas, and isolation levels. Define them in <code>.claude/agents/</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span><span style="color:#f92672">name</span>: <span style="color:#ae81ff">security-reviewer</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">description</span>: <span style="color:#ae81ff">Security audits, threat modeling, and OWASP analysis</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">model</span>: <span style="color:#ae81ff">claude-opus-4-6</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">color</span>: <span style="color:#ae81ff">red</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">isolation</span>: <span style="color:#ae81ff">worktree</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">memory</span>: <span style="color:#ae81ff">project</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">effort</span>: <span style="color:#ae81ff">high</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">background</span>: <span style="color:#66d9ef">false</span>
</span></span><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">You are an expert security engineer. Review code for OWASP Top 10, secrets exposure,</span>
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">injection vulnerabilities, and insecure data handling. Be specific about file paths,</span>
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">line numbers, and severity (critical / high / medium / low).</span>
</span></span></code></pre></div><p>Claude auto-invokes sub-agents based on task matching against their <code>description</code> field. You can also invoke them explicitly: <code>Use the security-reviewer agent to audit src/payments/</code>.</p>
<p>Two built-in sub-agents are always available:</p>
<ul>
<li><strong><code>Explore</code></strong> (Haiku, read-only) — fast codebase searches without touching your context window</li>
<li><strong><code>Plan</code></strong> (read-only) — planning and architecture reasoning without execution</li>
</ul>
<p><strong>Background agents</strong> — set <code>background: true</code> in the frontmatter. The agent runs in a separate session while you continue working. Manage background tasks with <code>/tasks</code>. Kill them with <code>Ctrl+F</code>.</p>
<p><strong>Agent Teams</strong> coordinate multiple sub-agents across parallel sessions for tasks that exceed a single context window. A lead agent decomposes the work, delegates to teammates, and synthesizes results. Useful for large refactors, cross-service audits, or any task where the codebase is too large to reason about in one pass.</p>
<hr>
<h2 id="plugins-and-mcp-servers">Plugins and MCP servers</h2>
<p>MCP (Model Context Protocol) servers extend Claude Code with tools beyond the built-in set — databases, external APIs, internal services, custom retrieval systems. Configure them in <code>.mcp.json</code> at the project root:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;mcpServers&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;postgres&#34;</span>: {
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;npx&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;args&#34;</span>: [<span style="color:#e6db74">&#34;-y&#34;</span>, <span style="color:#e6db74">&#34;@modelcontextprotocol/server-postgres&#34;</span>],
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;env&#34;</span>: {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;DATABASE_URL&#34;</span>: <span style="color:#e6db74">&#34;${DATABASE_URL}&#34;</span>
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    },
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;github&#34;</span>: {
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;npx&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;args&#34;</span>: [<span style="color:#e6db74">&#34;-y&#34;</span>, <span style="color:#e6db74">&#34;@modelcontextprotocol/server-github&#34;</span>],
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;env&#34;</span>: {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;GITHUB_TOKEN&#34;</span>: <span style="color:#e6db74">&#34;${GITHUB_TOKEN}&#34;</span>
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    },
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;internal-docs&#34;</span>: {
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;node&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;args&#34;</span>: [<span style="color:#e6db74">&#34;./scripts/mcp-docs-server.js&#34;</span>]
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Once configured, MCP tools appear alongside built-in tools in Claude&rsquo;s toolset. Claude discovers what each server offers and uses them when relevant — no additional prompting required.</p>
<p>Commit <code>.mcp.json</code>. Env vars reference environment variables rather than hardcoded values, so secrets stay out of the file. For project-specific internal tools (a docs server, a deployment trigger, a staging environment API), a small MCP server is almost always the right abstraction.</p>
<hr>
<h2 id="key-cli-flags">Key CLI flags</h2>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Non-interactive: run and exit (essential for CI)</span>
</span></span><span style="display:flex;"><span>claude -p <span style="color:#e6db74">&#34;run the test suite and summarise failures&#34;</span> --output-format json
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Resume the most recent conversation</span>
</span></span><span style="display:flex;"><span>claude -c
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Start in plan mode — read-only, no writes until you approve</span>
</span></span><span style="display:flex;"><span>claude --permission-mode plan
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Isolated git worktree (changes don&#39;t affect your working tree)</span>
</span></span><span style="display:flex;"><span>claude --worktree
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Override model for a session</span>
</span></span><span style="display:flex;"><span>claude --model claude-opus-4-6
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Set reasoning effort (Opus 4.6 only)</span>
</span></span><span style="display:flex;"><span>claude --effort high
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Hard cap on spend for an agentic run</span>
</span></span><span style="display:flex;"><span>claude --max-budget-usd 2.00
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Limit agentic turns (safety valve for headless runs)</span>
</span></span><span style="display:flex;"><span>claude --max-turns <span style="color:#ae81ff">30</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Force structured JSON output against a schema</span>
</span></span><span style="display:flex;"><span>claude -p <span style="color:#e6db74">&#34;...&#34;</span> --json-schema <span style="color:#e6db74">&#39;{&#34;type&#34;:&#34;object&#34;,&#34;properties&#34;:{&#34;verdict&#34;:{&#34;type&#34;:&#34;string&#34;}}}&#39;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --output-format json
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Enable real browser control via Playwright</span>
</span></span><span style="display:flex;"><span>claude --chrome
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Add context to the built-in prompt without replacing it</span>
</span></span><span style="display:flex;"><span>claude --append-system-prompt <span style="color:#e6db74">&#34;You are working on the payments service. Be conservative with schema changes.&#34;</span>
</span></span></code></pre></div><p><code>--append-system-prompt</code> is preferred over <code>--system-prompt</code>. The latter replaces the entire built-in prompt, which strips important behavioral scaffolding that Claude Code relies on internally.</p>
<hr>
<h2 id="cicd-and-headless-pipelines">CI/CD and headless pipelines</h2>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span>- <span style="color:#f92672">name</span>: <span style="color:#ae81ff">Claude review</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">env</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">ANTHROPIC_API_KEY</span>: <span style="color:#ae81ff">${{ secrets.ANTHROPIC_API_KEY }}</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">run</span>: |<span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    claude -p &#34;Review the diff for correctness, test coverage, and security issues.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">               Output JSON: {verdict, summary, issues[]}&#34; \
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">           --output-format json \
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">           --permission-mode plan \
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">           --max-turns 20 \
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">           --max-budget-usd 1.00 \
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    | tee review.json
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    jq -r &#39;.summary&#39; review.json
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    jq -e &#39;.verdict == &#34;approve&#34;&#39; review.json</span>
</span></span></code></pre></div><p>Use <code>stream-json</code> output format to process results line-by-line in long-running pipeline steps. Use <code>--max-turns</code> and <code>--max-budget-usd</code> together as a two-layer safety valve on every headless run.</p>
<hr>
<h2 id="the-file-structure-to-commit">The file structure to commit</h2>
<pre tabindex="0"><code>your-project/
├── CLAUDE.md                  # shared project brain — commit this
├── CLAUDE.local.md            # personal overrides — .gitignore this
├── TASKS.md                   # active long-run task plan — commit this
├── .mcp.json                  # MCP server config — commit this
└── .claude/
    ├── settings.json          # shared permissions and hooks — commit this
    ├── settings.local.json    # personal overrides — .gitignore this
    ├── agents/                # sub-agent definitions — commit these
    ├── commands/              # custom slash commands — commit these
    ├── skills/                # reusable instruction sets — commit these
    │   └── pr-review/
    │       └── SKILL.md
    ├── rules/                 # modular CLAUDE.md rules — commit these
    │   ├── api.md
    │   └── frontend/
    └── hooks/                 # optional shell scripts — commit these
</code></pre><p>Treat <code>.claude/</code>, <code>CLAUDE.md</code>, and <code>.mcp.json</code> like your CI configuration. They define how Claude behaves for everyone on the project. Review changes to them in PRs the same way you would review changes to <code>.github/workflows/</code>.</p>
<p>Run <code>/doctor</code> on any new machine to surface installation issues. Run <code>/memory</code> at the start of any new project to see what Claude has already loaded. Run <code>/init</code> if CLAUDE.md doesn&rsquo;t exist yet.</p>
<h2 id="next-step">Next step</h2>
<p>If your team is evaluating how to integrate Claude Code into an engineering workflow — alongside existing CI/CD, code review, and operational tooling — <a href="https://yottadynamics.com/#contact">book a discovery call</a> to talk through the specifics.</p>
]]></content:encoded></item></channel></rss>